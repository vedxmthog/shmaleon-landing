---
phase: 03-trust-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - legal.html
  - privacy.html
  - compliance.html
  - index.html
  - the-story.html
  - bespoke.html
  - .planning/phases/03-trust-compliance/03-TRUST-02-MEMO.md
autonomous: true
requirements:
  - TRUST-01
  - TRUST-02
user_setup: []
must_haves:
  truths:
    - legal.html, privacy.html, compliance.html exist and are linked from all pillar footers
    - No footer trust link uses href="#" after execution
    - Either draft gate visible on all three trust pages or explicit documented swap to published copy (one consistent mode)
  artifacts:
    - legal.html
    - privacy.html
    - compliance.html
    - 03-TRUST-02-MEMO.md
  key_links:
    - index|bespoke|the-story footers -> legal|privacy|compliance
---

<objective>
Implement TRUST-01: add three static trust/legal HTML pages, wire **Legal / Privacy / Compliance** footer links on **index.html**, **bespoke.html**, and **the-story.html** (remove `href="#"`). Use a visible **draft — not for publication** gate on each trust page until counsel-approved copy replaces it. TRUST-02: write short compliance review memo under this phase directory.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
| T-01 | False or misleading legal claims published | high | Draft banner on every trust page; memo records counsel review state — no removal without documented approval in TRUST-02 |
| T-02 | Broken internal links / mixed `#` | medium | acceptance_criteria grep for `href="#"` in trust link rows; grep for `legal.html` on all pillars |
| T-03 | Open redirect / unsafe target | low | Internal links relative `legal.html` only; `target="_blank"` not used for internal trust pages |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-trust-compliance/03-UI-SPEC.md
@.planning/phases/03-trust-compliance/03-RESEARCH.md
@.planning/phases/03-trust-compliance/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Add legal.html, privacy.html, compliance.html with shell + draft gate</name>
  <files>legal.html, privacy.html, compliance.html</files>
  <read_first>the-story.html, .planning/phases/03-trust-compliance/03-UI-SPEC.md, index.html</read_first>
  <action>
Create **three new files** in repo root: `legal.html`, `privacy.html`, `compliance.html`.

(1) **Head/body base:** Reuse the same document structure as `the-story.html`: `<!DOCTYPE html>`, `charset` UTF-8, viewport, Tailwind CDN, Google Fonts (Inter/Noto), Material Symbols if story uses them. Include a **header** with the same **primary nav** as `the-story.html` (links to `index.html`, `bespoke.html`, `the-story.html`) and correct **active** state on none or minimal (legal pages are not “story” — no nav pill for story on these, or match Phase 1 pattern for non-pillar: use neutral `text-neutral-400` for all three pillar links with no `border-b` active state).

(2) **Draft gate (TRUST-01):** Immediately inside `<body>`, before `<main>`, add a full-width banner `div` (e.g. `bg-amber-900/30 border-b border-amber-500/40 text-amber-100 text-center text-xs font-noto-serif py-2 px-4`) containing:
- EN: `Draft — not for publication`
- RU: `Черновик — не для публикации` inside `<span class="data-lang" data-lang="ru">` or the **same** `data-lang` pattern used on `index.html` for footers (use three `<span data-lang="en|ru|th">` in one `p` if that matches existing i18n layout on index).

(3) **Main:** `<main class="...">` with `<h1>` per page: English titles **Legal** / **Privacy** / **Compliance** (localized subheading via `data-lang` spans for RU/TH per 03-UI-SPEC). Body: placeholder paragraphs stating that final text will be provided by qualified counsel (do not assert legal compliance or health claims).

(4) **Footer:** Copy footer structure from `index.html` (SHMALEON, copyright block, social row, **Legal/Privacy/Compliance** links) — set trust links to `href="legal.html"`, `href="privacy.html"`, `href="compliance.html"`.

(5) **File names** must be exactly: `legal.html`, `privacy.html`, `compliance.html`.
  </action>
  <verify>Three files exist; draft banner string present; h1 per file</verify>
  <acceptance_criteria>
    - `test -f legal.html` and `test -f privacy.html` and `test -f compliance.html` exit 0
    - `rg -F 'Draft — not for publication' legal.html privacy.html compliance.html` — each file contains the EN draft string
    - `rg -F 'legal.html' legal.html` and same for `privacy.html`, `compliance.html` in footer sections (self-link OK)
    - `rg 'href="index.html"' legal.html` matches at least once; same for `bespoke.html` and `the-story.html` in each new file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Wire trust links in all pillar footers (remove #)</name>
  <files>index.html, the-story.html, bespoke.html</files>
  <read_first>index.html, the-story.html, bespoke.html, legal.html</read_first>
  <action>
(1) **index.html:** Find the three footer anchors that wrap Legal/Privacy/Compliance (currently `href="#"` around `data-lang` spans, approx. lines 571–585). Set `href="legal.html"`, `href="privacy.html"`, `href="compliance.html"` respectively (Legal first, Privacy second, Compliance third).

(2) **the-story.html:** Set the three trust links in the footer (currently `href="#"`) to `legal.html`, `privacy.html`, `compliance.html` in the same order.

(3) **bespoke.html:** Update the three `a` elements with `data-i18n="footer_legal"`, `footer_privacy`, `footer_compliance` to use `href="legal.html"`, `href="privacy.html"`, `href="compliance.html"` (not `#`).
  </action>
  <verify>rg href="#" limited to non-trust or zero in trust row</verify>
  <acceptance_criteria>
    - `rg 'href="legal.html"' bespoke.html` returns ≥1
    - `rg 'href="privacy.html"' bespoke.html` returns ≥1
    - `rg 'href="compliance.html"' bespoke.html` returns ≥1
    - `rg 'href="legal.html"' index.html` and `the-story.html` each returns ≥1
    - `rg 'href="privacy.html"' index.html` and `the-story.html` each returns ≥1
    - `rg 'href="compliance.html"' index.html` and `the-story.html` each returns ≥1
    - `rg 'href=\"legal\\.html\"' index.html bespoke.html the-story.html | wc -l` equals 3 (one per file)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>TRUST-02 — compliance review memo</name>
  <files>.planning/phases/03-trust-compliance/03-TRUST-02-MEMO.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, CLAUDE.md</read_first>
  <action>
Create **`.planning/phases/03-trust-compliance/03-TRUST-02-MEMO.md`** with markdown sections: **Jurisdiction** (e.g. Thailand as anchor market), **Scope reviewed** (list marketing surfaces: e.g. `index.html` cannabis-adjacent copy, `the-story.html`, `bespoke.html` product language — use concrete file names), **Method** (e.g. internal review + counsel TBD), **Outcome** (e.g. `Pending counsel review` or `No publication until draft removed`), **Open items** (what counsel must still approve). Do not invent a completed legal sign-off; state honest status. Link TRUST-01 draft posture: trust pages show draft banner until updated.
  </action>
  <verify>file exists; REQ TRUST-02 traceable</verify>
  <acceptance_criteria>
    - `test -f .planning/phases/03-trust-compliance/03-TRUST-02-MEMO.md` exits 0
    - File contains heading `#` or `##` and the substring `TRUST-02` or "Compliance review" or "Review outcome"
    - File lists at least `index.html` in body (grep-verifiable product-adjacent review scope)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `npx --yes serve -l 3456` — from `/`, open Legal/Privacy/Compliance; each loads; draft banner visible
- [ ] `the-story.html` and `bespoke.html` — footer trust links navigate to correct pages
- [ ] `rg 'href=\"#\"' index.html the-story.html bespoke.html` — confirm trust rows no longer use `#` (manual confirm if other `#` remain for other UI)
- [ ] Read `03-TRUST-02-MEMO.md` — outcome and open items are explicit
</verification>

---
phase: 09-production-social-urls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - social-urls.js
  - index.html
  - bespoke.html
  - the-story.html
  - legal.html
  - privacy.html
  - compliance.html
  - README.md
  - .planning/deploy.md
autonomous: true
requirements:
  - SOCIAL-01
user_setup:
  - "Brand: three public URLs (Instagram, Telegram, WhatsApp) or explicit approval to ship with social block hidden and zero TODO substrings in HTML/JS"
must_haves:
  truths:
    - No `instagram.com/TODO`, `t.me/TODO`, or `wa.me/TODO` in production-facing `*.html` or root-level `*.js` used for socials (verify via grep contract)
    - A single documented edit point for the three final URLs, or a documented hide flag with no user-visible TODO links
  artifacts:
    - social-urls.js
  key_links:
    - Footer social rows on all six public pages
---

<objective>
Ship **SOCIAL-01 (production)**: real Instagram, Telegram, and WhatsApp `href` targets for every footer, from **one** maintainable `social-urls.js`, **or** hide the three links and still remove all `TODO` URL substrings.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
| T-9-01 | User tricked to malicious `href` | medium | `href` values only in checked-in `social-urls.js` with `https://` to known hostnames; no interpolation from form/query |
| T-9-02 | Stale copy of URLs across files | low | One module + grep gate for `TODO` |
| T-9-03 | `target="_blank"` tab-nabbing | low | Keep `rel="noopener noreferrer"` on all three `<a>` |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-production-social-urls/09-CONTEXT.md
@.planning/phases/09-production-social-urls/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Add root social-urls module</name>
  <files>social-urls.js, index.html, bespoke.html, the-story.html, legal.html, privacy.html, compliance.html</files>
  <read_first>index.html (script block with `const SOCIAL` and `sync` function), 09-RESEARCH.md</read_first>
  <action>Create **new** file `social-urls.js` in repo root with an IIFE that sets **`window.SHMALEON_SOCIAL` = `{ instagram: 'https://…', telegram: 'https://…', whatsapp: 'https://…' }`**. If brand URLs are TBD, use a **`window.SHM_SOCIAL_ENABLED = false`** (boolean) in the same file and set each URL to empty string or keep placeholder boolean-only switch — **the shipped HTML/JS must not contain the literal substrings** `instagram.com/TODO`, `t.me/TODO`, `wa.me/TODO` (use a hide path that removes or does not render those hrefs). When URLs are known, set all three to real `https://` profile links and set **`SHM_SOCIAL_ENABLED: true`**. In each of the six HTML files: add **before** the inline block that currently defines `const SOCIAL`, a tag **`&lt;script src="social-urls.js"&gt;&lt;/script&gt;`**. Remove each duplicate **`const SOCIAL = { ... }`** and replace with **`const SOCIAL = window.SHMALEON_SOCIAL`** (or branch: if `!window.SHM_SOCIAL_ENABLED` then hide the footer social container via `classList.add('hidden')` on the known `id` for that page’s three-link row, per page—document ids in a one-line code comment). Update static **`href="https://…"`** on the three `<a>` in each file to match the same strings as in `social-urls.js` **or** set to `#` and rely on sync to set on DOMContentLoaded only if the hide path is not used — prefer **matching real URLs in HTML and JS** for consistency when enabled.</action>
  <verify>rg + load</verify>
  <acceptance_criteria>
    - `test -f social-urls.js` and file contains the substring `SHMALEON_SOCIAL` or `SHM_SOCIAL_ENABLED`
    - `rg "const SOCIAL = \{ instagram: 'https://instagram.com/TODO'"` returns no matches in `*.html`
    - `rg 'instagram\.com/TODO|t\.me/TODO|wa\.me/TODO' --glob '*.html' --glob '*.js'` from repo root returns **no** lines (or only in comments explicitly listing “forbidden” if you add a denylist comment — prefer zero)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Docs: single edit point + deploy</name>
  <files>README.md, .planning/deploy.md</files>
  <read_first>README.md (Bespoke / SOCIAL section), .planning/deploy.md (Pre-flight)</read_first>
  <action>Update **README.md** and **.planning/deploy.md** to state that **production social URLs** are edited **only in `social-urls.js`**, and list the two flags (`SHM_SOCIAL_ENABLED` and URL object) in one short bullet list. Remove any sentence that still says to replace `SOCIAL` in “each page script” as the **primary** method—point to `social-urls.js` first.</action>
  <verify>read</verify>
  <acceptance_criteria>
    - `rg -n 'social-urls\.js' README.md .planning/deploy.md` has at least one hit per file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Regression: bespoke footer + sync ids</name>
  <files>bespoke.html, legal.html, privacy.html, compliance.html, index.html, the-story.html</files>
  <read_first>Each file’s `function sync*Footer` and `getElementById` for footer link roots</read_first>
  <action>Ensure every **`sync*Footer*Social` / `SOCIAL` consumer** still runs after the global is defined: order of scripts must be **`social-urls.js` then** inline script. Verify **`footer-social`**, **`footer-social-legal`**, **`footer-social-privacy`**, **`footer-social-compliance`**, and bespoke/index/story **ids** unchanged unless hide logic requires a single `if (!SHM_SOCIAL_ENABLED) return;` at top of each sync.</action>
  <verify>manual + rg</verify>
  <acceptance_criteria>
    - `rg -c 'getElementById\(.footer-social' --glob '*.html'` shows each page still calls its footer root id
    - Opening `bespoke.html` in a browser with devtools: no `Uncaught ReferenceError: SHMALEON_SOCIAL` (or adjust name to match implementation)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `rg 'instagram\.com/TODO|t\.me/TODO|wa\.me/TODO' --glob '*.html' --glob '*.js'` — empty
- [ ] `npx --yes serve` — footer links work on `index.html` and one legal page (or block hidden with no TODO in view source)
</verification>

<success_criteria>
- SOCIAL-01 (production) satisfied: one edit surface, no production placeholder URLs
- Grep contract green on main marketing files
</success_criteria>

## PLANNING COMPLETE

1 plan, 1 wave, 3 tasks. Ready for `gsd-execute-phase 9` after brand URLs or hide decision.

---
phase: 08-home-hero-cta-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - HERO-CTA-01
user_setup: []
must_haves:
  truths:
    - Hero has two CTA links with resolvable href (bespoke + story) or documented single-CTA exception approved against roadmap
    - No dead hash-only navigation in hero CTA cluster
  artifacts:
    - index.html
  key_links:
    - index.html hero CTA group → bespoke.html
    - index.html hero CTA group → the-story.html
---

<objective>
Wire **`index.html` hero** so primary action opens **`bespoke.html`** and secondary action opens **`the-story.html`**, with EN|RU|TH labels. Satisfy HERO-CTA-01: no `href="#"` in that cluster.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
| T-01 | Open redirect or bad external URL in hero | low | Only relative `bespoke.html` and `the-story.html` in hero CTAs; no `javascript:` |
| T-02 | Phishing copy change | low | No change to host or protocol; static paths only |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-home-hero-cta-wiring/08-UI-SPEC.md
@.planning/phases/08-home-hero-cta-wiring/08-RESEARCH.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Dual hero CTA row + i18n</name>
  <files>index.html</files>
  <read_first>index.html (hero section from first &lt;section class="relative w-full min-h-0 min-h-[100svh] through closing &lt;/section&gt; of that hero), .planning/phases/08-home-hero-cta-wiring/08-UI-SPEC.md</read_first>
  <action>Inside the hero `div.mt-12.w-full.max-w-xl` (or equivalent container holding the single CTA at ~line 316): (1) Keep primary navigation to `href="bespoke.html"`. (2) Add a second `&lt;a href="the-story.html"&gt;` in the same flex row, using the same base button class stack as the primary (adjust layout with `flex flex-col min-[400px]:flex-row gap-4 justify-center` on the parent so two buttons fit; primary first). (3) Primary label sets: EN "Inquire", RU "Запросить" (or brand-approved RU for inquiry), TH "สอบถาม" (or brand-approved). Secondary: EN "Enter", RU "Войти" / narrative line per brand, TH "เข้าสู่" / short phrase. Use `data-lang="en"`, `data-lang="ru"`, `data-lang="th"` blocks mirroring the `h1` subline pattern (three spans per language per button, or one wrapper per language—match existing `index.html` language-toggle behavior). (4) Remove any `href="#"` in this section.</action>
  <verify>File read + click test</verify>
  <acceptance_criteria>
    - `index.html` between line 300 and 330 (hero section) contains exactly two `&lt;a` elements with `href="bespoke.html"` and `href="the-story.html"` respectively in the CTA block (or `rg -n 'min-h-\[100svh\]' index.html` then read until `&lt;/section&gt;` — hero contains both hrefs)
    - `rg 'href="#"' index.html` with manual confirmation: no match falls inside the hero CTA `div` (or zero `href="#"` in lines 300–330)
    - Each new label has three `data-lang` variants for primary and for secondary in the CTA group
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Scarcity block CTA check (non-regression)</name>
  <files>index.html</files>
  <read_first>index.html (scarcity section "15,000 THB" through its CTA)</read_first>
  <action>Confirm the scarcity section CTA (below hero) still has `href="bespoke.html"` and no `href="#"`. If copy still says "Join Priority List" only, leave as-is (different section); do not add dead links.</action>
  <verify>rg</verify>
  <acceptance_criteria>
    - `rg -n '15,000 THB' index.html -A20` shows one `href="bespoke.html"` in that block
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `npx --yes serve` (or static open): `/` — hero shows two buttons; "Inquire" → bespoke, "Enter" → story; repeat after `setLanguage` for RU and TH
- [ ] `rg 'href=\"#\"' index.html` count is 0, or not in hero
</verification>

<success_criteria>
- HERO-CTA-01 met: hero primary/secondary actions resolve in EN, RU, TH
- No regression to nav, locale, or footer scripts
</success_criteria>

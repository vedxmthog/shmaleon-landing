---
phase: 17-legal-page-density
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - legal.html
  - privacy.html
  - compliance.html
  - .planning/phases/17-legal-page-density/17-01-SUMMARY.md
autonomous: true
requirements:
  - TRUST-01
user_setup: []
must_haves:
  truths:
    - legal.html, privacy.html, compliance.html use the same responsive vertical spacing per 17-UI-SPEC
    - Main bottom padding is reduced below lg; lg+ keeps pb-section-padding
    - Draft banner, H1, and body paragraph stacks are tighter on mobile only
  artifacts:
    - legal.html
    - privacy.html
    - compliance.html
    - 17-01-SUMMARY.md
  key_links:
    - .planning/phases/17-legal-page-density/17-UI-SPEC.md
---

<objective>
Satisfy Phase 17 / TRUST-01: trust legal surfaces read with **tighter mobile vertical rhythm** (less “whitespace runaway” for short placeholder copy) by applying **identical** Tailwind utility updates to `legal.html`, `privacy.html`, and `compliance.html` per `17-UI-SPEC.md`. No copy, nav, or font-family changes.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-17-01 | Accidental removal of draft locale spans or draft copy | medium | acceptance_criteria grep for `data-lang` and `Draft — not for publication` on all three files after edit |
| T-17-02 | Inconsistent spacing between trust pages | low | One task edits all three files with the same class strings |
| T-17-03 | Broken `main` landmark or skip-link target | low | Do not change `id="main-content"` or `<a href="#main-content">` |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-legal-page-density/17-UI-SPEC.md
@.planning/phases/17-legal-page-density/17-RESEARCH.md
@.planning/phases/17-legal-page-density/17-PATTERNS.md
@legal.html
</context>

<tasks>

<task type="auto">
  <name>Mobile density — main + inner stack (legal, privacy, compliance)</name>
  <files>legal.html,privacy.html,compliance.html</files>
  <read_first>.planning/phases/17-legal-page-density/17-UI-SPEC.md,legal.html (lines containing main id="main-content" through first main-column closing div before footer),privacy.html (same region),compliance.html (same region)</read_first>
  <action>
In **each** of `legal.html`, `privacy.html`, `compliance.html`, make **only** these class substitutions (string-for-string on the specific elements — preserve all other attributes and inner HTML):

(1) **`<main id="main-content" ...>`** — Replace the `class` value’s padding tail segment so that the element includes exactly these utility classes for vertical padding: keep existing `pt-28 md:pt-32` (unchanged) and set bottom padding to **`pb-12 sm:pb-16 md:pb-24 lg:pb-section-padding`** (remove standalone `pb-section-padding` if present). If `class` is one long string, ensure no duplicate `pb-*` tokens remain.

(2) **Draft banner** — The first `div` inside the `max-w-3xl` column that contains `Draft — not for publication` (has `mb-10` and amber border classes): change **`mb-10`** to **`mb-6 sm:mb-8 md:mb-10`**. Leave `rounded-sm border border-amber-500/40` and inner `data-lang` spans unchanged.

(3) **H1** (Legal / Privacy / Compliance): change **`mb-8`** in that `h1` to **`mb-5 sm:mb-6 md:mb-8`**.

(4) **Each of the three `p` elements** in `main` with `font-body-md` and `mb-4`: change **`mb-4`** to **`mb-3 md:mb-4`**.

Do not modify footer, nav, scripts, or `tailwind.config` font entries.
  </action>
  <verify>rg + file read of main section</verify>
  <acceptance_criteria>
    - `rg 'pb-12 sm:pb-16 md:pb-24 lg:pb-section-padding' legal.html privacy.html compliance.html` prints three lines (one file per line in rg output) — i.e. each file contains the full substring
    - `rg 'mb-6 sm:mb-8 md:mb-10' legal.html privacy.html compliance.html` each returns ≥1 match
    - `rg 'mb-5 sm:mb-6 md:mb-8' legal.html privacy.html compliance.html` each returns ≥1 match
    - `rg 'mb-3 md:mb-4' legal.html privacy.html compliance.html` each returns ≥3 (three paragraphs)
    - `rg 'Draft — not for publication' legal.html privacy.html compliance.html` each ≥1
    - `rg 'data-lang="en"' legal.html` and same for `privacy.html`, `compliance.html` in the draft banner area — at least one hit per file
    - `rg 'id="main-content"' legal.html privacy.html compliance.html` — exactly one per file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>17-01-SUMMARY — mobile pass note</name>
  <files>.planning/phases/17-legal-page-density/17-01-SUMMARY.md</files>
  <read_first>$HOME/.claude/get-shit-done/templates/summary.md,.planning/phases/16-typography-pass-optional/16-01-SUMMARY.md</read_first>
  <action>
Create **`.planning/phases/17-legal-page-density/17-01-SUMMARY.md`**: list files touched; state **Phase 17** goal (tighter mobile rhythm on trust pages). Include **Manual UAT** — executor confirmed at **375px** (or iPhone preset) on `legal.html`: bottom padding no longer feels like excessive empty runaway after the last paragraph; same spot-check on `privacy.html` and `compliance.html`. **Deviations:** `None` or concrete note if any class had to be adjusted.
  </action>
  <verify>file exists</verify>
  <acceptance_criteria>
    - File `.planning/phases/17-legal-page-density/17-01-SUMMARY.md` exists
    - File contains the exact substring `375px` or the exact substring `iPhone`
    - File contains the phrase `legal.html` and lists all three trust HTML files by name
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] All acceptance_criteria commands from task 1 pass in repo root
- [ ] Browser: 375px width — legal, privacy, compliance; no horizontal scroll; draft banner + locales visible
</verification>

<success_criteria>
- Roadmap Phase 17: mobile legal pages read without excessive whitespace runaway; class/HTML tweaks only
- TRUST-01: presentation of trust surfaces remains defensible and readable
</success_criteria>

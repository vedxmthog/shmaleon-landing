---
phase: 14-a11y-follow-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - i18n.js
  - shm-chrome.css
  - index.html
  - bespoke.html
  - the-story.html
  - legal.html
  - privacy.html
  - compliance.html
  - .planning/GLOSSARY.md
  - .planning/phases/14-a11y-follow-up/14-VERIFICATION.md
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
user_setup: []
must_haves:
  truths:
    - Skip link is first focusable in body on index, bespoke, the-story, legal, privacy, compliance
    - Each of those pages has exactly one `id="main-content"` on its primary `<main>`
    - `prefers-reduced-motion` block exists in shm-chrome.css
    - All informative images on index and the-story have non-empty accurate `alt` matching `data-alt` where both exist
  artifacts:
    - i18n.js
    - shm-chrome.css
    - six HTML pages above
    - 14-VERIFICATION.md
  key_links:
    - skip_to_main → i18n.js → GLOSSARY.md
    - #main-content → first tab stop inside main after activate skip
---

<objective>
Ship Phase 14 **A11y follow-up**: localized **skip links**, **`main-content` landmark id**, **`prefers-reduced-motion`** in `shm-chrome.css`, **alt text** fixes on `index.html` and `the-story.html`, and a **documented keyboard verification** checklist. Meets **A11Y-01** and **A11Y-02** per `14-CONTEXT.md` / `14-UI-SPEC.md`.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-14-01 | XSS via translation | low | `skip_to_main` strings are static literals in `i18n.js`; `textContent` only in `shmApplyLocale` |
| T-14-02 | Open redirect via skip `href` | low | Fragment `#main-content` only; same page |
| T-14-03 | Focus trap / broken tab order | medium | Follow DOM order in CONTEXT; test per `14-VERIFICATION.md` |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-a11y-follow-up/14-CONTEXT.md
@.planning/phases/14-a11y-follow-up/14-UI-SPEC.md
@.planning/phases/14-a11y-follow-up/14-RESEARCH.md
@.planning/phases/14-a11y-follow-up/14-PATTERNS.md
@i18n.js
@shm-chrome.css
@index.html
</context>

<tasks>

<task type="auto">
  <name>skip_to_main i18n + glossary</name>
  <files>i18n.js, .planning/GLOSSARY.md</files>
  <read_first>i18n.js (full `SHM_TRANSLATIONS` en/ru/th objects through `index_scarcity_price_qualifier` or last key), .planning/GLOSSARY.md (table tail for column alignment)</read_first>
  <action>
**A. `i18n.js` —** Add key **`skip_to_main`** to **`en`**, **`ru`**, and **`th`** with these **exact** values:

- **en:** `Skip to main content`
- **ru:** `К основному содержанию`
- **th:** `ข้ามไปยังเนื้อหาหลัก`

Place the key in the same relative position in all three objects (e.g. after `nav_story`).

**B. `.planning/GLOSSARY.md` —** Append one table row: **`skip_to_main`** with EN / RU / TH cells matching the three strings above character-for-character.
  </action>
  <verify>node --check; rg</verify>
  <acceptance_criteria>
    - Shell: `node --check i18n.js` exits 0
    - `rg "skip_to_main" i18n.js` shows exactly three occurrences of the key name in object literals (en, ru, th) or equivalent; en value contains `Skip to main content`
    - `i18n.js` contains the substring `К основному содержанию` and `ข้ามไปยังเนื้อหาหลัก` in the ru and th blocks respectively
    - `.planning/GLOSSARY.md` contains `skip_to_main` in a row and that row’s cells match the three strings exactly
  </acceptance_criteria>
</task>

<task type="auto">
  <name>shm-skip-link + reduced-motion CSS</name>
  <files>shm-chrome.css</files>
  <read_first>shm-chrome.css (entire file)</read_first>
  <action>
**A. Skip link** — Append a **`.shm-skip-link`** rule block that:

- Off-screen / visually hidden by default (e.g. `position: absolute; left: -9999px; top: 0; width: 1px; height: 1px; overflow: hidden` or equivalent WCAG-friendly pattern).
- On **`:focus` and `:focus-visible`**: positions the link **fixed** or **absolute** at **`top: 1rem; left: 1rem`**, **`z-index` ≥ 100**, restores width/height/auto, padding **`0.5rem 0.75rem`** (8px / 12px), **`background`** matching dark chrome (e.g. `#121414` or `var` if unused — use solid `#121414`), **text** light (`#e2e2e2`), **`border: 1px solid #e9c176`** (secondary), **`text-decoration: none`**, **`font-size: 12px`**, **`font-weight: 500`**.

**B. Reduced motion** — Append:

```css
@media (prefers-reduced-motion: reduce) {
  html:focus-within {
    scroll-behavior: auto;
  }
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

(If `scroll-behavior` is not set on `html`, the `html:focus-within` rule may be omitted — keep the universal reduced-motion block.)

**C.** End file with a one-line comment: `/* Phase 14: skip link + prefers-reduced-motion */`
  </action>
  <verify>rg</verify>
  <acceptance_criteria>
    - `shm-chrome.css` contains the substring `.shm-skip-link`
    - `shm-chrome.css` contains `prefers-reduced-motion`
    - `shm-chrome.css` contains `Phase 14: skip link` or `Phase 14: skip link + prefers-reduced-motion` in a comment
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Skip link + main id on six pages</name>
  <files>index.html, bespoke.html, the-story.html, legal.html, privacy.html, compliance.html</files>
  <read_first>Each file: first 15 lines after `&lt;body` through start of `&lt;nav&gt;`; locate existing `&lt;main` opening tag</read_first>
  <action>
For **each** of the six files:

1. Change **first** `&lt;main` to **`&lt;main id="main-content"`** (preserve existing attributes/classes).
2. **Immediately before** the first **`&lt;nav`** in `&lt;body&gt;`, insert one anchor (first focusable in body):

```html
<a href="#main-content" class="shm-skip-link focus:outline-none focus-visible:ring-2 focus-visible:ring-secondary focus-visible:ring-offset-2 focus-visible:ring-offset-background rounded"><span data-i18n="skip_to_main">Skip to main content</span></a>
```

(Adjust only if Tailwind classes must wrap differently — keep classes `shm-skip-link` plus the same **focus-visible ring** stack as `index-nav-toggle` in `index.html`.)

3. Do **not** add a second `id="main-content"`.
4. **`the-collection.html`** is out of scope (redirect stub).

Apply file-specific nav id prefixes are unchanged — only insert skip before that page’s top `nav`.
  </action>
  <verify>rg</verify>
  <acceptance_criteria>
    - `rg -l 'shm-skip-link' --glob '*.html'` lists exactly: `index.html`, `bespoke.html`, `the-story.html`, `legal.html`, `privacy.html`, `compliance.html`
    - For each of those six files: `rg -n 'id="main-content"' FILENAME` returns exactly one match on a `main` open tag
    - `rg -n '<body' index.html -A3` shows `shm-skip-link` appears before `<nav` (use `read` or rg context to confirm order on each file)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Alt text sync index + story</name>
  <files>index.html, the-story.html</files>
  <read_first>grep or read every `&lt;img` line in both files</read_first>
  <action>
For **every** `&lt;img` in **`index.html`** that has **`data-alt`** and **`alt=""`**: set **`alt`** to the **exact** string in **`data-alt`** (duplicate value; keep `data-alt` so `shmHydrateImageAlts` stays consistent).

For **every** `&lt;img` in **`the-story.html`** that has **`data-alt`** and empty or missing meaningful **`alt`**: set **`alt`** to the **exact** `data-alt` string.

Do not change **`bespoke.html`** images in this task (already have non-empty `alt`).

Do not remove `data-alt` attributes.
  </action>
  <verify>rg</verify>
  <acceptance_criteria>
    - `rg '<img alt=""' index.html` returns **no matches**
    - `rg '<img alt=""' the-story.html` returns **no matches**
    - `index.html` still contains **5** `&lt;img` tags (same count as before — `rg -c '<img' index.html` equals 5)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Keyboard verification doc</name>
  <files>.planning/phases/14-a11y-follow-up/14-VERIFICATION.md</files>
  <read_first>.planning/phases/14-a11y-follow-up/14-UI-SPEC.md (Focus section)</read_first>
  <action>
Create or overwrite **`14-VERIFICATION.md`** with:

- Title `# Phase 14 — Keyboard &amp; skip verification`
- **Date:** 2026-04-27
- **Pages checklist:** `index.html`, `bespoke.html`, `the-story.html`, `legal.html`, `privacy.html`, `compliance.html` — each with checkboxes: `[ ]` Skip link visible on Tab focus; `[ ]` Enter on skip moves to main; `[ ]` Tab order: header controls then first CTA/content in main matches UI-SPEC for that page; `[ ]` Escape closes mobile nav/locale if open
- **Reduced motion:** `[ ]` OS reduce-motion on — transitions feel instant
- **Sign-off:** line `Executor: __________  Date: __________`

No placeholder lorem — use the checklist text above verbatim structure.
  </action>
  <verify>test -f</verify>
  <acceptance_criteria>
    - File `.planning/phases/14-a11y-follow-up/14-VERIFICATION.md` exists
    - File contains the substring `Phase 14 — Keyboard` or `Keyboard & skip verification`
    - File lists all six HTML filenames in the checklist body
    - File contains `Reduced motion` as a subsection or bullet group
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `node --check i18n.js` passes
- [ ] Manual: complete `14-VERIFICATION.md` checklists in a real browser (keyboard-only pass per page)
- [ ] Optional: `Reduce motion` OS setting — reload home — hero/nav transitions not noticeably slow
</verification>

<success_criteria>
- ROADMAP success criterion: **documented pass** for skip link, reduced-motion, focus order hero + nav — satisfied by `14-VERIFICATION.md` + implemented code
- **A11Y-01:** Meaningful alts on index/story catalog imagery
- **A11Y-02:** Skip link, focusable chrome, operable hero CTAs via keyboard
</success_criteria>

## PLANNING COMPLETE

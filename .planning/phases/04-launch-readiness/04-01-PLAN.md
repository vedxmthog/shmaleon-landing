---
phase: 04-launch-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .planning/deploy.md
  - .planning/phases/04-launch-readiness/04-SEO-CHECKLIST.md
  - .planning/phases/04-launch-readiness/04-ORGANIZATION-JSON-LD-TEMPLATE.json
  - index.html
  - bespoke.html
  - the-story.html
  - the-collection.html
  - legal.html
  - privacy.html
  - compliance.html
autonomous: true
requirements:
  - REL-01
  - REL-02
  - SEO-01
user_setup: []
must_haves:
  truths:
    - REL-01 satisfied via README + `.planning/deploy.md` with `/` → index, placeholder replacement, and static-host assumptions
    - Every public HTML file has unique title, meta description, og:title, og:description, og:type, og:url; canonical uses `__CANONICAL_ORIGIN__` until replaced
    - SEO-01 checklist exists; internal pillar + trust links are hrefs to real `.html` files; JSON-LD is template-only until approved
  artifacts:
    - .planning/deploy.md
    - 04-SEO-CHECKLIST.md
    - 04-ORGANIZATION-JSON-LD-TEMPLATE.json
  key_links:
    - index ↔ bespoke ↔ the-story ↔ legal ↔ privacy ↔ compliance
---

<objective>
Deliver REL-01 deploy documentation, REL-02 per-page metadata and Open Graph tags, and SEO-01 canonical URLs + heading/link hygiene + optional JSON-LD kept as a non-served template until brand/legal approves structured data.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
| T-01 | Production traffic sent to wrong origin via canonical/og:url | medium | Single placeholder `__CANONICAL_ORIGIN__`; deploy.md requires replace-before-launch; checklist grep for leftover placeholder |
| T-02 | Misleading social preview copy | low | OG description strings reviewed with marketing; static literals only — no user input in meta |
| T-03 | Broken internal links at go-live | medium | Checklist + `rg` for required `href="*.html"` targets; trust pages included after Phase 3 |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-launch-readiness/04-RESEARCH.md
@.planning/phases/04-launch-readiness/04-PATTERNS.md
@.planning/phases/04-launch-readiness/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>REL-01 — deploy.md and README release engineering</name>
  <files>.planning/deploy.md, README.md</files>
  <read_first>README.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <action>
Create **`.planning/deploy.md`** with markdown sections: **Hosting model** (static files only; repo root is web root), **Local verify** (`npx --yes serve -l 8080` from repo root), **Production URLs** (document **`__CANONICAL_ORIGIN__`** — literal string to find-replace with final origin, no trailing slash), **Index / collection** (`/` must resolve to `index.html`; `the-collection.html` is legacy redirect), **Pre-flight placeholders** (point to README or REQUIREMENTS for `BESPOKE_MAILTO`, `SOCIAL` URL map), **GitHub Pages project site** (optional subsection: `https://&lt;user&gt;.github.io/&lt;repo&gt;/` and relative links), **Rollback** (revert to previous static deploy).

Update **`README.md`** **Deploy (brief)** subsection: add one sentence that full steps live in `.planning/deploy.md` and that `__CANONICAL_ORIGIN__` must be replaced in all HTML before production.
  </action>
  <verify>test -f .planning/deploy.md; rg -n __CANONICAL_ORIGIN__ README.md .planning/deploy.md</verify>
  <acceptance_criteria>
    - `test -f .planning/deploy.md` exits 0
    - `rg -F '__CANONICAL_ORIGIN__' .planning/deploy.md` returns ≥1 match
    - `rg -F '.planning/deploy.md' README.md` returns ≥1 match
    - `rg -F 'index.html' .planning/deploy.md` returns ≥1 match (collection home / `/` behavior)
    - `rg -F 'serve' .planning/deploy.md` returns ≥1 match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>REL-02 — meta description and Open Graph on all public pages</name>
  <files>index.html, bespoke.html, the-story.html, the-collection.html, legal.html, privacy.html, compliance.html</files>
  <read_first>index.html, bespoke.html, the-story.html, the-collection.html, legal.html, privacy.html, compliance.html</read_first>
  <action>
In **each** listed file, inside `<head>`, **after** `viewport` meta and **before** the first `<script` or `<link` to Tailwind, insert:

(1) **Unique** `<meta name="description" content="..."/>` using these **exact** English strings (one per file):
- `index.html`: `SHMALEON collection — precision pastry and botanical craft in Phuket. Explore the line and join the priority list.`
- `bespoke.html`: `Configure a bespoke SHMALEON experience — channel, contact, and concierge hand-off. Invitation-only.`
- `the-story.html`: `The SHMALEON story — ritual, restraint, and craft anchored in Phuket.`
- `the-collection.html`: `Redirect to the SHMALEON collection home.`
- `legal.html`: `Legal information for SHMALEON — draft page; not for publication until counsel-approved.`
- `privacy.html`: `Privacy information for SHMALEON — draft page; not for publication until counsel-approved.`
- `compliance.html`: `Product compliance and disclaimers for SHMALEON — draft page; not for publication until counsel-approved.`

(2) **Open Graph** block immediately after description. For every file use:
- `meta property="og:type" content="website"`
- `meta property="og:title"` — **exact** `content` per file: `SHMALEON - The Collection` (index); `SHMALEON - Bespoke Configuration` (bespoke); `SHMALEON - The Story` (story); `SHMALEON — Redirect` (the-collection); for `legal.html` / `privacy.html` / `compliance.html` use `og:title` equal to those files’ existing `&lt;title&gt;` after any edit you make for uniqueness (minimum: `SHMALEON - Legal`, `SHMALEON - Privacy`, `SHMALEON - Compliance`).
- `meta property="og:description" content="..."` — **duplicate** that file’s meta description string verbatim.
- `meta property="og:url"` — absolute URL with placeholder, **same path as canonical**: `index.html` and `the-collection.html` both use `content="__CANONICAL_ORIGIN__/index.html"`; `bespoke.html` → `__CANONICAL_ORIGIN__/bespoke.html`; `the-story.html` → `__CANONICAL_ORIGIN__/the-story.html`; `legal.html` → `__CANONICAL_ORIGIN__/legal.html`; `privacy.html` → `__CANONICAL_ORIGIN__/privacy.html`; `compliance.html` → `__CANONICAL_ORIGIN__/compliance.html`.

Do **not** add `og:image` unless an approved absolute image URL exists; note omission in `04-SEO-CHECKLIST.md` under manual follow-ups.
  </action>
  <verify>rg 'og:title' on each file; descriptions distinct</verify>
  <acceptance_criteria>
    - `rg -l 'meta name="description"' index.html bespoke.html the-story.html the-collection.html legal.html privacy.html compliance.html | wc -l` outputs `7`
    - `rg -c 'meta property="og:title"' index.html bespoke.html the-story.html the-collection.html legal.html privacy.html compliance.html` shows count `1` for each file (exactly one og:title per file)
    - `rg -F 'SHMALEON collection — precision pastry' index.html` returns ≥1
    - `rg -F 'Configure a bespoke SHMALEON' bespoke.html` returns ≥1
    - `rg -F 'The SHMALEON story' the-story.html` returns ≥1
    - `rg -F '__CANONICAL_ORIGIN__' index.html bespoke.html the-story.html the-collection.html legal.html privacy.html compliance.html | wc -l` is ≥7 (og:url contains placeholder)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>SEO-01 — canonical links, checklist, link graph, JSON-LD template</name>
  <files>index.html, bespoke.html, the-story.html, the-collection.html, legal.html, privacy.html, compliance.html, .planning/phases/04-launch-readiness/04-SEO-CHECKLIST.md, .planning/phases/04-launch-readiness/04-ORGANIZATION-JSON-LD-TEMPLATE.json</files>
  <read_first>.planning/deploy.md, 04-RESEARCH.md, index.html, bespoke.html, the-story.html, the-collection.html, legal.html, privacy.html, compliance.html</read_first>
  <action>
(1) **Canonical:** In each of the seven HTML files, add **one** `<link rel="canonical" href="..."/>` in `<head>`. Use **only** this mapping (placeholder origin, no trailing slash on origin):
- `index.html` → `href="__CANONICAL_ORIGIN__/index.html"`
- `the-collection.html` → `href="__CANONICAL_ORIGIN__/index.html"` (same as collection home)
- `bespoke.html` → `href="__CANONICAL_ORIGIN__/bespoke.html"`
- `the-story.html` → `href="__CANONICAL_ORIGIN__/the-story.html"`
- `legal.html` / `privacy.html` / `compliance.html` → `href="__CANONICAL_ORIGIN__/legal.html"` (etc.)

Remove any **older** relative canonical in `the-collection.html` (`href="index.html"`) when inserting the new tag.

(2) **Single h1:** For each file, ensure exactly **one** `&lt;h1` in the document (if trust pages have multiple, consolidate to one primary; if none, add one consistent with page purpose).

(3) **Create** `.planning/phases/04-launch-readiness/04-SEO-CHECKLIST.md` with checkboxes: placeholder replaced (grep zero `__CANONICAL_ORIGIN__`), all pages served 200, titles unique, descriptions unique, canonical matches live URL, internal nav links include `index.html`, `bespoke.html`, `the-story.html`, `legal.html`, `privacy.html`, `compliance.html` where applicable, no `href="#"` on footer Legal/Privacy/Compliance rows.

(4) **Create** `.planning/phases/04-launch-readiness/04-ORGANIZATION-JSON-LD-TEMPLATE.json` as **valid JSON** with `@context`, `@type` `Organization`, `name` `SHMALEON`, `url` `__CANONICAL_ORIGIN__/`, and `description` a neutral one-line brand line. Add top comment is invalid in JSON — instead first line in file is not possible; put a sibling **`04-ORGANIZATION-JSON-LD-README.txt`** one line: `Not loaded by the site — embed in index.html only after legal approval.` **If** README.txt is too many files, add instead a markdown note at bottom of `04-SEO-CHECKLIST.md`: "JSON-LD template not wired; do not script-load until approved."

**Amendment:** Only create the `.json` template; add the "not wired" sentence to **`04-SEO-CHECKLIST.md`** (no extra README file).

(5) **Link audit command** documented in checklist: `rg -n 'href="#"' index.html bespoke.html the-story.html legal.html privacy.html compliance.html` — executor records result; **target** is zero matches **or** list of allowed exceptions (e.g. no exceptions for trust/footer nav).

  </action>
  <verify>seven canonical links; checklist file exists; json template exists</verify>
  <acceptance_criteria>
    - `rg -c 'rel="canonical"' index.html bespoke.html the-story.html the-collection.html legal.html privacy.html compliance.html` shows count `1` per file
    - `rg -F '__CANONICAL_ORIGIN__' index.html bespoke.html the-story.html the-collection.html legal.html privacy.html compliance.html | wc -l` is ≥7
    - `test -f .planning/phases/04-launch-readiness/04-SEO-CHECKLIST.md` exits 0
    - `rg -F 'JSON-LD' .planning/phases/04-launch-readiness/04-SEO-CHECKLIST.md` returns ≥1
    - `test -f .planning/phases/04-launch-readiness/04-ORGANIZATION-JSON-LD-TEMPLATE.json` exits 0
    - `rg -F '"@type": "Organization"' .planning/phases/04-launch-readiness/04-ORGANIZATION-JSON-LD-TEMPLATE.json` returns ≥1
    - From repo root: `node -e "JSON.parse(require('fs').readFileSync('.planning/phases/04-launch-readiness/04-ORGANIZATION-JSON-LD-TEMPLATE.json','utf8'))"` exits 0
  </acceptance_criteria>
</task>

</tasks>

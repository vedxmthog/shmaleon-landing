---
phase: 13-price-clarity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - i18n.js
  - index.html
  - .planning/GLOSSARY.md
autonomous: true
requirements:
  - I18N-01
  - I18N-02
  - "(commerce-adjacent clarity)"
user_setup: []
must_haves:
  truths:
    - Scarcity block shows `15,000 THB` then the qualifier line then "Limited Weekly Production" in all locales
    - `index_scarcity_price_qualifier` exists in `SHM_TRANSLATIONS.en`, `.ru`, and `.th`
  artifacts:
    - i18n.js
    - index.html
  key_links:
    - index.html scarcity → `i18n.js` `index_scarcity_price_qualifier` → GLOSSARY row
---

<objective>
Add **one line of per-unit price microcopy** under **`15,000 THB`** on the collection home (`index.html`) with **EN / RU / TH** strings in **`i18n.js`** and **`GLOSSARY.md`**, so the THB amount is unambiguous in every locale.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-13-01 | HTML injection via translation | low | Keys are static in `i18n.js` only; no `innerHTML` for this line |
| T-13-02 | Omitted locale key → blank line under price | low | Add same key to `en`, `ru`, and `th` in one change |
| T-13-03 | Accidental change to "Limited Weekly" copy | low | Edits only the new paragraph between `h2` and the existing italic `p` |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/I18N-PATTERN.md
@.planning/phases/13-price-clarity/13-UI-SPEC.md
@.planning/phases/13-price-clarity/13-RESEARCH.md
@i18n.js
@index.html
</context>

<tasks>

<task type="auto">
  <name>Qualifer copy + i18n + index markup</name>
  <files>i18n.js, index.html</files>
  <read_first>index.html (scarcity section from the `15,000 THB` h2 through the next two paragraphs; lines ~525–535), i18n.js (full `SHM_TRANSLATIONS` en/ru/th blocks, `shmApplyLocale`), .planning/I18N-PATTERN.md</read_first>
  <action>
**A. `i18n.js` —** Add to **`en`**, **`ru`**, and **`th`** the key **`index_scarcity_price_qualifier`** with these exact string values (including punctuation):

- **en:** `Per piece.`
- **ru:** `За единицу.`
- **th:** `ต่อชิ้น`

Place the key in the same relative position in all three objects (e.g. after `footer_compliance` or in a short `// index` comment block if the file uses section comments; do not duplicate the key name).

**B. `index.html` —** In the section whose `h2` text is `15,000 THB`, **insert** a new **single** `<p>` **immediately after** the `</h2>` that contains `15,000 THB` and **immediately before** the existing `<p class="... italic">` that contains "Limited Weekly Production" / the `data-lang` triplet. The new element:

- `data-i18n="index_scarcity_price_qualifier"`
- Body text (fallback) **`Per piece.`** to match `SHM_TRANSLATIONS.en`
- Classes: use secondary body styling consistent with 13-UI-SPEC (e.g. `font-body-sm` or `text-sm`, `text-on-surface-variant`, `mt-2` or `mb-1` for spacing) — do not use the same `italic` or `text-body-lg` as the "Limited Weekly" line; this is subordinate.

**C.** Do not edit the "Limited Weekly Production" paragraph content or the CTA in this task.
  </action>
  <verify>node --check; grep</verify>
  <acceptance_criteria>
    - Shell: `node --check i18n.js` exits 0
    - `i18n.js` contains the substring `index_scarcity_price_qualifier` followed (within the `en` object) by a value that includes `Per piece.`
    - `i18n.js` contains `За единицу` and `ต่อชิ้น` in the `ru` and `th` objects respectively (same key name `index_scarcity_price_qualifier` in each)
    - `index.html` contains exactly one attribute `data-i18n="index_scarcity_price_qualifier"`
    - `index.html` file order: after the line with `15,000 THB` and its closing `h2`, the next user-visible text container is the new `p` with `index_scarcity_price_qualifier`, then the italic limited-production `p` (use read_file or `rg -n` to confirm order of lines in the scarcity section)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Glossary row</name>
  <files>.planning/GLOSSARY.md</files>
  <read_first>.planning/GLOSSARY.md (table of keys through `footer_compliance`)</read_first>
  <action>Append one row: **| index_scarcity_price_qualifier | Per piece. | За единицу. | ต่อชิ้น |** (or align columns with the table’s existing spacing). EN/RU/TH cells must match `i18n.js` exactly for this key.</action>
  <verify>read</verify>
  <acceptance_criteria>
    - `.planning/GLOSSARY.md` contains the substring `index_scarcity_price_qualifier` in a table row
    - The same row’s EN/RU/TH cells match the three `index_scarcity_price_qualifier` strings in `i18n.js` character-for-character
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Browser: `index.html` — switch locale RU, TH, EN; line under `15,000 THB` updates and is readable (no overlap with CTA on mobile width)
- [ ] Confirm semantic matches 13-UI-SPEC: per piece / per unit, not a "from" range
</verification>

<success_criteria>
- **Commerce-adjacent clarity:** No ambiguity that `15,000 THB` refers to a per-piece starting reference in all three languages.
- **I18N-01 / I18N-02:** No hard-coded RU/TH English for this string; copy tracked in GLOSSARY.
</success_criteria>

## PLANNING COMPLETE

---
phase: 02-content-i18n-accessibility
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01-PLAN.md
files_modified:
  - i18n.js
  - .planning/I18N-RESOLUTION.md
  - index.html
  - bespoke.html
  - the-story.html
autonomous: true
requirements:
  - I18N-04
user_setup: []
must_haves:
  truths:
    - First visit without shm_locale uses navigator.languages mapping (ru-*→ru, th-*→th, else en)
    - After explicit user choice, shm_locale wins on return
    - Resolution order documented in .planning/I18N-RESOLUTION.md
  artifacts:
    - .planning/I18N-RESOLUTION.md
  key_links:
    - i18n.js implements getInitialLocale per doc
---

<objective>
Implement **automatic locale on first visit** and **sticky user choice** via `localStorage` key `shm_locale`, with safe fallbacks when storage is unavailable; document resolution order for operators.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
| T-01 | Locale fingerprinting | low | Same-origin localStorage only; no third-party sync |
| T-02 | Crash on private mode | low | try/catch around all Storage access |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-content-i18n-accessibility/02-RESEARCH.md
@.planning/phases/02-content-i18n-accessibility/02-01-PLAN.md
@i18n.js
@index.html
@bespoke.html
@the-story.html
</context>

<tasks>

<task type="auto">
  <name>Navigator mapping + getInitialLocale</name>
  <files>i18n.js</files>
  <read_first>i18n.js,.planning/REQUIREMENTS.md</read_first>
  <action>Add `window.shmGetInitialLocale(): 'en'|'ru'|'th'` implementing order: (1) `const stored = window.shmReadStoredLocale();` if non-null return it; (2) iterate `navigator.languages || [navigator.language]` (if `navigator` missing, return `'en'`); for each tag, lower-case, if `tag === 'th' || tag.startsWith('th-')` return `'th'`; if `tag === 'ru' || tag.startsWith('ru-')` return `'ru'`; (3) return `'en'`. Ensure `shmReadStoredLocale` returns null when key absent or Storage throws.</action>
  <verify>node -c i18n.js 2>/dev/null || true</verify>
  <acceptance_criteria>
    - `rg -n "navigator\\.languages|navigator\\.language" i18n.js` returns at least one match
    - `rg -n "shmGetInitialLocale|getInitialLocale" i18n.js` returns at least one match (exact name must match what you export — if using `shmGetInitialLocale`, grep that)
    - `i18n.js` contains substring `th-` and substring `ru-` (prefix checks)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Wire all pages to initial locale</name>
  <files>index.html,bespoke.html,the-story.html</files>
  <read_first>i18n.js,index.html,bespoke.html,the-story.html</read_first>
  <action>Replace every `DOMContentLoaded` path that still does `shmApplyLocale('en')` or `shmApplyLocale(s || 'en')` **only** with `shmApplyLocale(window.shmGetInitialLocale())` (or equivalent single call). Language **buttons** must call `shmPersistLocale(lang)` then `shmApplyLocale(lang)`. Create `.planning/I18N-RESOLUTION.md` with numbered list: (1) User explicit choice in `localStorage` key `shm_locale`; (2) If absent, `navigator.languages` mapping per REQUIREMENTS; (3) Fallback `en`; (4) Note try/catch behavior when Storage disabled.</action>
  <verify>rg DOMContentLoaded index.html bespoke.html the-story.html</verify>
  <acceptance_criteria>
    - `.planning/I18N-RESOLUTION.md` exists and contains substring `shm_locale`
    - `.planning/I18N-RESOLUTION.md` contains substring `navigator`
    - `rg -n "shmGetInitialLocale" index.html bespoke.html the-story.html` ≥ 3 matches total across files
  </acceptance_criteria>
</task>

</tasks>

---
phase: 02-content-i18n-accessibility
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02-PLAN.md
files_modified:
  - i18n.js
  - index.html
  - bespoke.html
  - the-story.html
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
user_setup: []
must_haves:
  truths:
    - Meaningful images have accurate alt; decorative use empty alt
    - Burger + locale disclosure expose aria-expanded; form errors use aria-invalid + describedby and localized strings
  artifacts:
    - i18n.js
  key_links:
    - Keyboard tab reaches bespoke submit and footer links
---

<objective>
Polish **accessibility**: image `alt`, heading sanity, visible focus on custom controls, `aria-expanded` on burger and locale disclosure, and **localized** inline validation for bespoke contact (FORM-01) with assistive feedback.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
| T-01 | PII in live region | low | Only validation messages; no full handle logged |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-content-i18n-accessibility/02-UI-SPEC.md
@.planning/phases/01-site-shell-navigation/01-02-PLAN.md
@index.html
@bespoke.html
@the-story.html
</context>

<tasks>

<task type="auto">
  <name>Images and headings audit</name>
  <files>index.html,bespoke.html,the-story.html</files>
  <read_first>index.html,the-story.html,.planning/REQUIREMENTS.md</read_first>
  <action>For each **meaningful** catalog/hero `<img>` that currently has `alt=""` but includes `data-alt=...` on same element: set **runtime** `alt` from `data-alt` in a small DOMContentLoaded snippet in `i18n.js` or page script (e.g. `document.querySelectorAll('img[data-alt]').forEach(img => { img.alt = img.getAttribute('data-alt'); });`). For purely decorative images, remove misleading `data-alt` or keep `alt=""` only. **the-story.html:** replace the three stacked locale `h1` elements with **exactly one** `h1` (use `data-i18n="story_hero_title"` or equivalent key in `SHM_TRANSLATIONS`; `shmApplyLocale` updates text). Keep **one** `h1` on `index.html` and `bespoke.html` (already single).</action>
  <verify>rg '<h1' the-story.html</verify>
  <acceptance_criteria>
    - `index.html` contains substring `data-alt` implies at least one script line setting `img.alt` OR static `alt=` non-empty for main catalog/hero images: `rg -n "img\\.alt|setAttribute\\(\\s*['\"]alt['\"]" index.html i18n.js` ≥ 1 **or** `rg '<img[^>]+alt=\"[^\"]+\"' index.html` matches product section images with non-empty alt
    - `rg -c '<h1' the-story.html` outputs exactly `1`
    - `rg -c '<h1' index.html` outputs exactly `1`
    - `rg -c '<h1' bespoke.html` outputs exactly `1`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Focus and disclosure ARIA</name>
  <files>index.html,the-story.html,bespoke.html</files>
  <read_first>.planning/phases/01-site-shell-navigation/01-02-PLAN.md,index.html,the-story.html,bespoke.html</read_first>
  <action>On **burger** button and **locale** disclosure trigger (Phase 1 markup): add `aria-expanded="false"` initially; toggle to `true`/`false` when panel opens/closes. Add `aria-controls` pointing to panel `id` (add `id` if missing). Add Tailwind `focus-visible:ring-2 focus-visible:ring-secondary focus-visible:ring-offset-2 focus-visible:ring-offset-background` (or equivalent existing tokens) to burger, locale button, and bespoke primary CTA. Ensure mobile menu panel has `role="dialog"` or `role="navigation"` per Phase 1 plan — do not regress Phase 1 behavior.</action>
  <verify>rg aria-expanded index.html the-story.html bespoke.html</verify>
  <acceptance_criteria>
    - `rg -n "aria-expanded" index.html the-story.html bespoke.html` ≥ 3 matches total
    - `rg -n "aria-controls" index.html the-story.html bespoke.html` ≥ 3 matches total
    - `rg -n "focus-visible:ring" index.html the-story.html bespoke.html` ≥ 2 matches total
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Bespoke form validation i18n + a11y</name>
  <files>bespoke.html,i18n.js</files>
  <read_first>bespoke.html,.planning/REQUIREMENTS.md,.planning/phases/02-content-i18n-accessibility/02-UI-SPEC.md</read_first>
  <action>Add translation keys for **each** validation error (e.g. `error_email_invalid`, `error_whatsapp_format`, `error_telegram_required`, `error_channel_required`) in **all three** languages inside `SHM_TRANSLATIONS`. On validate/submit: set input `aria-invalid="true|false"`, `aria-describedby` pointing to error span `id`; error span text from `shmApplyLocale` active language. Add `&lt;div id="form-status" role="status" aria-live="polite" class="sr-only"&gt;&lt;/div&gt;` or visible region for submit outcome. Ensure channel selector labels are associated with inputs via `for`/`id`.</action>
  <verify>rg aria-invalid bespoke.html</verify>
  <acceptance_criteria>
    - `bespoke.html` contains substring `aria-invalid`
    - `bespoke.html` contains substring `aria-describedby`
    - `i18n.js` contains substring `error_` (validation key prefix) in at least 2 lines
    - `bespoke.html` contains `role="status"` OR `aria-live="polite"`
  </acceptance_criteria>
</task>

</tasks>

---
phase: 02-content-i18n-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - i18n.js
  - .planning/GLOSSARY.md
  - .planning/I18N-PATTERN.md
  - index.html
  - the-story.html
  - bespoke.html
autonomous: true
requirements:
  - I18N-01
  - I18N-02
  - I18N-05
user_setup: []
must_haves:
  truths:
    - One documented i18n pattern links shared module + glossary
    - No mixed bespoke data-i18n vs index data-lang without a bridging layer in i18n.js
    - RU/TH cover all user-visible strings on index, story, bespoke including nav and footer
  artifacts:
    - i18n.js
    - .planning/GLOSSARY.md
    - .planning/I18N-PATTERN.md
  key_links:
    - Glossary nav_bespoke matches bespoke nav label in RU and TH
---

<objective>
Unify internationalization into **one maintainable mechanism**: shared `i18n.js`, glossary-aligned EN/RU/TH strings, and full coverage on `index.html`, `the-story.html`, and `bespoke.html` (no untranslated nav islands or duplicate conflicting Russian labels).
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
| T-01 | XSS via injected locale strings | low | Only static keys in repo; no `innerHTML` from user input for i18n |
| T-02 | Broken nav after refactor | low | Keep relative `href` values; only text nodes change |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-content-i18n-accessibility/02-UI-SPEC.md
@.planning/phases/02-content-i18n-accessibility/02-RESEARCH.md
@.planning/phases/02-content-i18n-accessibility/02-PATTERNS.md
@index.html
@the-story.html
@bespoke.html
</context>

<tasks>

<task type="auto">
  <name>Glossary + i18n module + pattern doc</name>
  <files>i18n.js,.planning/GLOSSARY.md,.planning/I18N-PATTERN.md</files>
  <read_first>bespoke.html,.planning/REQUIREMENTS.md,.planning/phases/02-content-i18n-accessibility/02-RESEARCH.md,index.html,the-story.html</read_first>
  <action>Create **root** `i18n.js` that defines: `const SHM_LOCALE_STORAGE_KEY = 'shm_locale'`; `const SHM_TRANSLATIONS = { en: {...}, ru: {...}, th: {...} }` containing **every** `data-i18n` key currently in bespoke.html (nav_collection through footer_compliance) plus keys needed for index/story chrome strings. Align Russian **bespoke nav** with index Russian nav: use **one** RU string for “Bespoke” everywhere (pick from glossary row `nav_bespoke` after updating `.planning/GLOSSARY.md`). Export globals: `window.shmApplyLocale(lang)`, `window.shmPersistLocale(lang)` (storage try/catch), `window.shmReadStoredLocale()` returning `'en'|'ru'|'th'|null`. Implement `shmApplyLocale` to: (1) update all `[data-i18n]` text and `[data-i18n-placeholder]` placeholders from `SHM_TRANSLATIONS[lang]`; (2) set `document.documentElement.className` to strip `/\blang-\w+\b/g` and add `lang-${lang}` for pages using CSS `data-lang` hiding (index pattern). For `[data-lang]` elements: hide non-matching via class `hidden` OR rely on existing `.lang-xx [data-lang]` CSS — **document chosen rule** in `.planning/I18N-PATTERN.md`. Write `.planning/GLOSSARY.md` as markdown table: Key | EN | RU | TH. Write `.planning/I18N-PATTERN.md` describing file load order, API names, and glossary update process.</action>
  <verify>test -f i18n.js && test -f .planning/GLOSSARY.md && test -f .planning/I18N-PATTERN.md</verify>
  <acceptance_criteria>
    - `rg -n "SHM_LOCALE_STORAGE_KEY|shm_locale" i18n.js` returns at least one match
    - `rg -n "nav_collection|footer_compliance" i18n.js` returns matches
    - `.planning/GLOSSARY.md` contains markdown header `#` or `##` and the substring `nav_bespoke`
    - `.planning/I18N-PATTERN.md` contains substring `i18n.js`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Wire bespoke.html to shared i18n</name>
  <files>bespoke.html</files>
  <read_first>bespoke.html,i18n.js</read_first>
  <action>Insert **before** existing inline script: `&lt;script src="i18n.js"&gt;&lt;/script&gt;`. Remove the inline `const translations = { ... };` object entirely. Replace `setLanguage(lang)` body with `window.shmPersistLocale(lang); window.shmApplyLocale(lang);` (or call exported names exactly as defined in i18n.js). Ensure `document.getElementById('current-lang')` still updates if that element exists after Phase 1; if missing, skip or guard with null check. Add `DOMContentLoaded` listener: `const s = window.shmReadStoredLocale(); window.shmApplyLocale(s || 'en');` and update `#current-lang` text to `(s || 'en').toUpperCase()`.</action>
  <verify>rg data-i18n bespoke.html | head</verify>
  <acceptance_criteria>
    - `bespoke.html` contains literal substring `src="i18n.js"`
    - `bespoke.html` does **not** contain substring `const translations = {` (removed)
    - `bespoke.html` contains substring `shmApplyLocale` or `shmPersistLocale`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Unify index.html and the-story.html</name>
  <files>index.html,the-story.html</files>
  <read_first>index.html,the-story.html,i18n.js,.planning/GLOSSARY.md</read_first>
  <action>Add `&lt;script src="i18n.js"&gt;&lt;/script&gt;` in `head` or end of body **before** inline locale scripts. **index.html:** Remove duplicate inline `setLanguage` that conflicts with `i18n.js`; on `DOMContentLoaded` call `window.shmApplyLocale(window.shmReadStoredLocale() || 'en')` and sync `.lang-btn` active classes exactly as today (map `data-target-lang` buttons). Preserve existing CSS block that hides `[data-lang]` per `.lang-*`. **the-story.html:** Replace hardcoded English nav link text (`The Collection`, `Bespoke`, `The Story`) with spans using same `data-lang` triplets as index OR `data-i18n` keys present in `SHM_TRANSLATIONS`. Replace `switchLanguage` implementation with delegation to `window.shmPersistLocale` + `window.shmApplyLocale` OR delete inline function if i18n.js provides story-specific hook — goal: **one code path** documented in I18N-PATTERN. Ensure visible content has RU/TH for nav + footer + body (already mostly present on story).</action>
  <verify>rg 'href="index.html"' the-story.html</verify>
  <acceptance_criteria>
    - `index.html` contains substring `src="i18n.js"`
    - `the-story.html` contains substring `src="i18n.js"`
    - `the-story.html` desktop nav `href="index.html"` anchor does **not** contain raw text `The Collection` as direct text node (use `data-i18n="nav_collection"` on anchor or inner `span` triplets)
    - `rg -n "nav_collection|data-lang=\"en\">The Collection" the-story.html` shows `nav_collection` **or** span-based localization (executor verifies zero English-only pillar labels when RU active)
  </acceptance_criteria>
</task>

</tasks>

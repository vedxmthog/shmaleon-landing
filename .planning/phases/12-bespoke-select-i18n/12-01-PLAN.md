---
phase: 12-bespoke-select-i18n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - i18n.js
  - bespoke.html
  - .planning/GLOSSARY.md
autonomous: true
requirements:
  - I18N-01
  - I18N-05
user_setup: []
must_haves:
  truths:
    - All four channel `<option>` elements under `#contact-channel` carry `data-i18n` keys present in `SHM_TRANSLATIONS.en|ru|th`
    - Option `value` attributes remain `telegram`, `instagram`, `whatsapp`, `email`
  artifacts:
    - i18n.js
    - bespoke.html
  key_links:
    - bespoke.html `#contact-channel` options → `i18n.js` `channel_option_*`
---

<objective>
Localize **bespoke** channel `<select>` options (EN/RU/TH) using **`data-i18n`** and **`SHM_TRANSLATIONS`**, and record copy in **`GLOSSARY.md`**, satisfying **I18N-01** and **I18N-05** for this control.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|----------|----------|------------|
| T-01 | Changing `value=` breaks validation or hand-off | low | Edit labels only; keep `value` literals unchanged |
| T-02 | Missing locale key leaves blank option text | low | Add all four keys to `en`, `ru`, and `th` in one edit |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/I18N-PATTERN.md
@.planning/phases/12-bespoke-select-i18n/12-UI-SPEC.md
@.planning/phases/12-bespoke-select-i18n/12-RESEARCH.md
@.planning/phases/12-bespoke-select-i18n/12-PATTERNS.md
@i18n.js
@bespoke.html
</context>

<tasks>

<task type="auto">
  <name>Translations + option markup + locale sync</name>
  <files>i18n.js, bespoke.html</files>
  <read_first>i18n.js (`SHM_TRANSLATIONS` en/ru/th blocks and `shmApplyLocale`), bespoke.html (`#contact-channel` through `</select>`, and `setLanguage` / `applyContactChannel`), .planning/I18N-PATTERN.md</read_first>
  <action>**A. `i18n.js`:** In each of `en`, `ru`, and `th`, add exactly these keys after `contact_channel_label` (or adjacent bespoke step4 keys): `channel_option_telegram`, `channel_option_instagram`, `channel_option_whatsapp`, `channel_option_email`. Use this copy:

**en:** Telegram | Instagram | WhatsApp | Email

**ru:** Telegram | Instagram | WhatsApp | Эл. почта

**th:** Telegram | Instagram | WhatsApp | อีเมล

**B. `bespoke.html`:** On the four `<option>` nodes inside `#contact-channel`, add `data-i18n="channel_option_telegram"` (etc.) matching `value`; keep English text as fallback body matching `en` table. Do not change `value` attributes.

**C. `bespoke.html`:** After `applyContactChannel` function is defined (and before or inside `DOMContentLoaded`), register `window.addEventListener('shm:locale', () => applyContactChannel());` so `#contact-label` and placeholders stay synced if `shmApplyLocale` runs without `setLanguage`.</action>
  <verify>node --check; grep</verify>
  <acceptance_criteria>
    - Shell: `node --check i18n.js` exits 0
    - `bespoke.html` contains exactly four `data-i18n="channel_option_` attribute assignments (one per option) on the channel select
    - `bespoke.html` still contains `value="telegram"`, `value="instagram"`, `value="whatsapp"`, `value="email"` on those options
    - `i18n.js` contains each of `channel_option_telegram`, `channel_option_instagram`, `channel_option_whatsapp`, `channel_option_email` inside **en**, **ru**, and **th** object literals
    - `bespoke.html` contains substring `shm:locale` and a call to `applyContactChannel` in that listener
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Glossary mirror</name>
  <files>.planning/GLOSSARY.md</files>
  <read_first>.planning/GLOSSARY.md (rows around `contact_channel_label`)</read_first>
  <action>Append four rows to the glossary table (after `contact_channel_label` row), columns **key | EN | RU | TH**, matching the exact strings used in `i18n.js` for `channel_option_*`.</action>
  <verify>read</verify>
  <acceptance_criteria>
    - `.planning/GLOSSARY.md` contains the substring `channel_option_telegram` and `channel_option_email` in table rows
    - Four consecutive keys `channel_option_telegram` through `channel_option_email` are documented with EN/RU/TH columns aligned to `i18n.js`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Browser: `bespoke.html` — switch to RU and TH; each option in Channel dropdown shows the localized label from the plan
- [ ] Change channel after locale switch; detail label still matches channel + locale
</verification>

<success_criteria>
- **I18N-01 / I18N-05:** No English-only hard-coded option labels when RU/TH active; mechanism matches documented `data-i18n` pattern
</success_criteria>

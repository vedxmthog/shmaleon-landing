---
phase: 18-dead-class-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - i18n.js
  - index.html
  - bespoke.html
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - i18n.js contains no `getElementById('current-lang')` and no `data-lang-btn` in `shmSyncLangButtons` attribute fallback chain
    - index.html loads Material Symbols stylesheet exactly once (one `fonts.googleapis.com` link containing `Material+Symbols+Outlined`)
    - bespoke.html inline `<style>` does not define `.pb-safe`
    - `node --check i18n.js` exits 0
  artifacts:
    - i18n.js
    - index.html
    - bespoke.html
  key_links:
    - .planning/phases/18-dead-class-cleanup/18-UI-SPEC.md
---

<objective>
Remove **dead i18n hooks** (`#current-lang`, `data-lang-btn` fallback, redundant locale-label ID updates), **dedupe** the duplicate Material Symbols `<link>` on `index.html`, and delete the **unused** `.pb-safe` rule from `bespoke.html`. Align with Phase 18 roadmap — no unused or misleading utility/CSS/JS pairs.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-18-01 | Locale labels stop updating on mobile | low | Keep `[data-shm-locale-label]` loop intact; only remove redundant lines that duplicate it |
| T-18-02 | Syntax error in i18n | low | Run `node --check i18n.js` before commit |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-dead-class-cleanup/18-UI-SPEC.md
@.planning/phases/18-dead-class-cleanup/18-RESEARCH.md
@.planning/phases/18-dead-class-cleanup/18-PATTERNS.md
@i18n.js
@index.html
@bespoke.html
</context>

<tasks>

<task type="auto">
  <name>Prune dead branches in i18n.js</name>
  <files>i18n.js</files>
  <read_first>i18n.js (functions shmSyncLangButtons, shmApplyLocale), .planning/phases/18-dead-class-cleanup/18-RESEARCH.md</read_first>
  <action>In **`shmSyncLangButtons`**: change the line that reads `btn.getAttribute('data-target-lang') || btn.getAttribute('data-lang-btn')` to **only** `btn.getAttribute('data-target-lang')` (no `data-lang-btn` fallback). In **`shmApplyLocale`**, remove the block that does `var cur = document.getElementById('current-lang'); if (cur) cur.textContent = ...` entirely. Remove the **redundant** lines that set `index-locale-label` and `story-locale-label` by ID **after** the `document.querySelectorAll('[data-shm-locale-label]').forEach(...)` loop (those spans already have `data-shm-locale-label` in HTML). Do not remove the `forEach` on `[data-shm-locale-label]`.</action>
  <verify>node --check</verify>
  <acceptance_criteria>
    - `rg "current-lang" i18n.js` returns **no** lines
    - `rg "data-lang-btn" i18n.js` returns **no** lines
    - `rg "index-locale-label|story-locale-label" i18n.js` returns **no** lines
    - Shell `node --check i18n.js` exits with code **0**
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Dedupe Material Symbols link on index.html</name>
  <files>index.html</files>
  <read_first>index.html first 20 lines (two consecutive Material Symbols links)</read_first>
  <action>In **`index.html` `<head>`**, delete **one** duplicate `<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:...` line so **exactly one** such link remains (same URL as kept copy).</action>
  <verify>rg count</verify>
  <acceptance_criteria>
    - `rg -n 'Material\+Symbols\+Outlined' index.html` outputs **exactly one** line number
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Remove unused .pb-safe from bespoke.html</name>
  <files>bespoke.html</files>
  <read_first>bespoke.html lines 104–108 (inline style)</read_first>
  <action>Remove the **entire** CSS rule `.pb-safe { padding-bottom: env(safe-area-inset-bottom); }` and its line from the inline `<style>` block. If the `<style>` becomes empty except whitespace, remove the `<style>...</style>` wrapper entirely; if `.pb-safe` was the only rule, delete the whole block.</action>
  <verify>grep</verify>
  <acceptance_criteria>
    - `rg 'pb-safe' bespoke.html` returns **no** output
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Execution SUMMARY</name>
  <files>.planning/phases/18-dead-class-cleanup/18-01-SUMMARY.md</files>
  <read_first>$HOME/.claude/get-shit-done/templates/summary.md</read_first>
  <action>Create **`18-01-SUMMARY.md`** listing files touched, bullets for removed dead hooks, confirmation duplicate font link removed, `.pb-safe` removed. **Manual UAT:** locale toggle on index + bespoke still updates labels. **Deviations:** `None` or note.</action>
  <verify>file exists</verify>
  <acceptance_criteria>
    - Path `.planning/phases/18-dead-class-cleanup/18-01-SUMMARY.md` exists
    - File contains substring `i18n.js` and `Material` or `Material Symbols`
    - File contains `Manual UAT` as a heading or bold label with at least one sentence
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `node --check i18n.js`
- [ ] `rg 'slow fade|slow-fade' --glob '*.{html,css,js}' .` — no misleading legacy class string (expect empty at repo root deliverables)
- [ ] Spot-check EN/RU/TH on index and bespoke
</verification>

<success_criteria>
- Roadmap Phase 18: JS/CSS match behavior; no dead hooks identified in research left in place
- Phase 5 dependency: no unrelated feature work
</success_criteria>

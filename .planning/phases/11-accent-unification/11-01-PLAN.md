---
phase: 11-accent-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - the-story.html
  - .planning/phases/01-site-shell-navigation/01-PATTERNS.md
  - bespoke.html
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - No `#C5A059` or `border-[#C5A059]` / `text-[#C5A059]` / `hover:bg-[#C5A059]` in repo-root `*.html`
    - Story closing CTA uses `border-secondary`, `text-secondary`, `hover:bg-secondary`, `hover:text-tertiary-container`
  artifacts:
    - the-story.html
  key_links:
    - the-story.html closing CTA → bespoke.html (unchanged href)
---

<objective>
Replace **ad-hoc gold** on **`the-story.html`** closing CTA with **design-token utilities** (`secondary`, `tertiary-container`) so accents match the rest of the site; run a **repo-wide HTML audit** for stray `#C5A059`; update stale **planning doc** that still cites the old hex.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|----------|----------|------------|
| T-01 | User confuses CTA for phishing if href changes | low | Do not change `href` or host; color/class only |
| T-02 | Contrast regression on hover | low | Use same hover pair as `index.html` scarcity CTA (`secondary` + `tertiary-container`) |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-accent-unification/11-UI-SPEC.md
@.planning/phases/11-accent-unification/11-RESEARCH.md
@.planning/phases/11-accent-unification/11-PATTERNS.md
@the-story.html
@index.html
</context>

<tasks>

<task type="auto">
  <name>Story page CTA token swap</name>
  <files>the-story.html</files>
  <read_first>the-story.html (search for `C5A059` or `Join the Priority List` CTA anchor), .planning/phases/11-accent-unification/11-UI-SPEC.md, index.html (scarcity section anchor with `href="bespoke.html"` and `border-secondary` — use as class reference for hover behavior)</read_first>
  <action>On the **single** `&lt;a href="bespoke.html"` that wraps “Join the Priority List” / RU / TH spans (~line 370): (1) Remove **all** `border-[#C5A059]`, `text-[#C5A059]`, `hover:bg-[#C5A059]`, `hover:text-black`. (2) Apply: `border border-secondary text-secondary` plus existing non-color classes (`px-8 min-[400px]:px-16 py-6 font-label-caps text-label-caps uppercase tracking-widest transition-all duration-500 w-full max-w-md inline-flex justify-center items-center text-center`). (3) Set hover to `hover:bg-secondary hover:text-tertiary-container` (match index scarcity CTA). (4) Append focus ring: `focus:outline-none focus-visible:ring-2 focus-visible:ring-secondary focus-visible:ring-offset-2 focus-visible:ring-offset-background`. Do not change `href`, inner spans, or copy.</action>
  <verify>grep + read</verify>
  <acceptance_criteria>
    - `the-story.html` contains **no** substring `C5A059` (case-insensitive)
    - That anchor’s `class` attribute contains the contiguous tokens `border-secondary`, `text-secondary`, `hover:bg-secondary`, `hover:text-tertiary-container`
    - Same anchor still has `href="bespoke.html"`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>HTML audit + planning doc drift</name>
  <files>.planning/phases/01-site-shell-navigation/01-PATTERNS.md</files>
  <read_first>repo root listing of `*.html`; .planning/phases/01-site-shell-navigation/01-PATTERNS.md</read_first>
  <action>(1) From repo root, search all `*.html` files (not under `.planning/`) for `C5A059` — expect **zero** matches after task 1. If any match remains, fix or document deviation in SUMMARY. (2) In `.planning/phases/01-site-shell-navigation/01-PATTERNS.md`, update the CTAs bullet that mentions `#C5A059` on story so it states **`border-secondary` / `text-secondary`** (token) instead of the obsolete hex. (3) Optional (if zero extra scope): on `bespoke.html` submit button only, replace `hover:text-[#0A0A0A]` with `hover:text-tertiary-container` for token parity; if skipped, note “not changed” in SUMMARY.</action>
  <verify>grep</verify>
  <acceptance_criteria>
    - Shell from repo root: `grep -r "C5A059" --include="*.html" . 2>/dev/null | grep -v "^\./\.planning/"` returns **no lines** (empty output)
    - `01-PATTERNS.md` does not contain the substring `C5A059`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Browser: `the-story.html` — CTA visible; hover shows gold fill with dark label; keyboard focus shows ring
- [ ] `grep -ri "c5a059" --include="*.html" .` — no hits outside `.planning/` (planning docs may mention historically; prefer no hits in deliverable HTML)
</verification>

<success_criteria>
- Roadmap Phase 11 success: **no stray hex gold** on Story CTA; peers use **`secondary`** utilities consistently on that surface
- **Depends on Phase 6** respected (no layout refactor)
</success_criteria>

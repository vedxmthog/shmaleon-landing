---
phase: 15-media-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - media/README.md
  - media/shm-index-hero.jpg
  - media/shm-ingot.jpg
  - media/shm-pulse.jpg
  - media/shm-heart-cake.jpg
  - media/shm-delivery.jpg
  - media/shm-bespoke-hero.jpg
  - media/shm-heart-product.jpg
  - media/shm-story-hero.jpg
  - media/shm-story-sourcing.jpg
  - media/shm-story-infusion.jpg
  - media/shm-story-presentation.jpg
  - media/shm-story-handover.jpg
  - index.html
  - bespoke.html
  - the-story.html
autonomous: true
requirements:
  - A11Y-01
must_haves:
  truths:
    - No `<img src="https://lh3.googleusercontent.com/...">` remains on index, bespoke, or story pages
    - Twelve files exist under `media/` with `shm-` prefix (plus README)
  artifacts:
    - media/*
    - index.html
    - bespoke.html
    - the-story.html
  key_links:
    - 15-RESEARCH.md § Media slug map
user_setup: []
---

<objective>
Self-host all **12 unique** Google `lh3.googleusercontent.com` **content images** from `index.html`, `bespoke.html`, and `the-story.html` under **`media/`**, replace **`src`** attributes with **relative paths**, and document provenance in **`media/README.md`**.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-15-01 | Supply-chain: malicious binary if URL hijacked | low | Download only from current committed HTML URLs; verify image opens locally |
| T-15-02 | Broken relative paths when site deployed under subpath | low | Document in README that deploy root must map `/` to repo root (existing static assumption) |
| T-15-03 | `curl` to untrusted host | low | Host is Google CDN; no execution of downloaded content |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-media-hardening/15-RESEARCH.md
@.planning/phases/15-media-hardening/15-PATTERNS.md
@index.html
@bespoke.html
@the-story.html
</context>

<tasks>

<task type="auto">
  <name>Create media directory and README</name>
  <files>media/README.md</files>
  <read_first>.planning/phases/15-media-hardening/15-RESEARCH.md (§ Media slug map and Findings)</read_first>
  <action>
Create directory **`media/`** at repository root (alongside `index.html`).

Create **`media/README.md`** containing:
- One-line purpose: self-hosted SHMALEON marketing imagery, migrated from Google CDN URLs (Phase 15).
- A **markdown table** with columns **`slug`**, **`output_file`**, **`pages`** listing all **12** rows from 15-RESEARCH § Media slug map, with `output_file` exactly:
  - `shm-index-hero.jpg`
  - `shm-ingot.jpg`
  - `shm-pulse.jpg`
  - `shm-heart-cake.jpg`
  - `shm-delivery.jpg`
  - `shm-bespoke-hero.jpg`
  - `shm-heart-product.jpg`
  - `shm-story-hero.jpg`
  - `shm-story-sourcing.jpg`
  - `shm-story-infusion.jpg`
  - `shm-story-presentation.jpg`
  - `shm-story-handover.jpg`
- A final row or note: source URLs are the **exact** `src` values from git `index.html` / `bespoke.html` / `the-story.html` at plan execution time (copy full URLs into README **once** for audit trail, or paste per-slug on the same table under column **`source_url`** — required for legal/provenance).

**Important:** If `curl` saves a non-JPEG (e.g. PNG/WebP), either re-encode to JPEG for the filename above **or** update `output_file` column and all HTML `src` in the next task to use the **same** extension consistently (no mixed broken references).
  </action>
  <verify>test -f</verify>
  <acceptance_criteria>
    - Shell: `test -d media` exits 0
    - `media/README.md` exists and contains the substring `shm-index-hero.jpg`
    - `media/README.md` contains all twelve slugs: `shm-index-hero`, `shm-ingot`, `shm-pulse`, `shm-heart-cake`, `shm-delivery`, `shm-bespoke-hero`, `shm-heart-product`, `shm-story-hero`, `shm-story-sourcing`, `shm-story-infusion`, `shm-story-presentation`, `shm-story-handover`
    - `media/README.md` contains the substring `https://lh3.googleusercontent.com/` at least once (documenting source URLs)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Download twelve images with curl</name>
  <files>media/shm-*.jpg</files>
  <read_first>index.html, bespoke.html, the-story.html (every line containing `src="https://lh3.googleusercontent.com`), media/README.md</read_first>
  <action>
For **each** of the 12 unique URLs, run **`curl -fL --fail`** saving to the path **`media/<output_file>`** from the README table. Use the **exact** URL string currently in the HTML `src` attribute (no truncation).

Suggested one-liner pattern (repeat per file with correct `-o`):
`curl -fL -o media/shm-ingot.jpg '<PASTE_EXACT_SRC_URL_FROM_HTML>'`

If any download fails with HTTP 4xx/5xx: stop, record the failing URL in `media/README.md` under **## Blocked downloads**, and do not commit broken HTML — escalate per executor playbook.

After all succeed: run **`file media/shm-*.jpg`** (or matching glob) and confirm each reports an image type (JPEG/PNG/WebP). If type is not JPEG, normalize filenames/extensions per README and note the change.
  </action>
  <verify>file, wc</verify>
  <acceptance_criteria>
    - Shell: `test -f media/shm-index-hero.jpg` and `test -f media/shm-story-handover.jpg` both exit 0 (adjust extension if README uses non-.jpg consistently)
    - Shell: `ls media/shm-* 2>/dev/null | wc -l` outputs `12`
    - Shell: `file media/shm-index-hero.jpg` contains `JPEG` OR `PNG` OR `WebP` (image data detected)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Replace img src with relative media paths</name>
  <files>index.html, bespoke.html, the-story.html</files>
  <read_first>index.html, bespoke.html, the-story.html (full file), media/README.md</read_first>
  <action>
In **`index.html`**, replace **`src`** on content images only (not script/link) so that:
- The hero background image that has `data-alt="Glowing black box in dark moody lighting"` uses **`src="media/shm-index-hero.jpg"`** (match extension to actual saved file).
- The image with `data-alt="Gold Ingot"` → **`media/shm-ingot.jpg`**
- The image with `data-alt="24k gold leaf Napoleon"` → **`media/shm-pulse.jpg`**
- The image with `data-alt="Heart-shaped cake"` → **`media/shm-heart-cake.jpg`**
- The image with `data-alt="White-glove delivery"` → **`media/shm-delivery.jpg`**

In **`bespoke.html`**:
- Hero `data-alt` containing `artisan hands` / craftsman → **`media/shm-bespoke-hero.jpg`**
- `alt="The Ingot"` tile → **`media/shm-ingot.jpg`**
- `alt="The Pulse"` tile → **`media/shm-pulse.jpg`**
- `alt="The Heart"` tile → **`media/shm-heart-product.jpg`**

In **`the-story.html`**:
- Hero `data-alt` starting `A cinematic, low-light photograph of a mysterious` → **`media/shm-story-hero.jpg`**
- Ritual step 1 `data-alt` starting `Close up of manicured hands` → **`media/shm-story-sourcing.jpg`**
- Step 2 `data-alt` starting `Macro shot of dark liquid infusing` → **`media/shm-story-infusion.jpg`**
- Step 4 `data-alt` starting `A perfectly arranged, minimalist presentation` → **`media/shm-story-presentation.jpg`**
- Handover `data-alt` starting `Two sets of hands in elegant attire` → **`media/shm-story-handover.jpg`**

**Do not** remove `data-alt` yet (Plan 02 may use it). **Do not** change Tailwind classes in this task.

Use the **same** relative path string (including extension) for every occurrence that shared the same remote URL (ingot/pulse duplicated across index and bespoke).
  </action>
  <verify>rg</verify>
  <acceptance_criteria>
    - Shell: `rg 'src="https://lh3\.googleusercontent\.com' index.html bespoke.html the-story.html` exits **1** (no matches; ripgrep exits 1 when no match)
    - `index.html` contains exactly one substring `src="media/shm-index-hero.jpg"` OR the same with the actual extension chosen in README
    - `index.html` contains `src="media/shm-ingot.jpg"` (or consistent extension)
    - `bespoke.html` contains `src="media/shm-bespoke-hero.jpg"` (or consistent extension)
    - `the-story.html` contains `src="media/shm-story-hero.jpg"` (or consistent extension)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Open all three HTML files in browser from disk; every image visible; no broken-image icon
- [ ] Network tab: no requests to `lh3.googleusercontent.com` for images
</verification>

<success_criteria>
- **Roadmap:** No fragile hot-linked **content** images in hero/catalogue/story — all use **`media/`** paths.
- **A11Y-01:** Foundation for image stability (local assets); Plan 02 completes dimensions and alt text.
</success_criteria>

## PLANNING COMPLETE

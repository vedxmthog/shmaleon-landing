---
phase: 10-claims-counsel-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - i18n.js
  - .planning/phases/10-claims-counsel-alignment/10-TRUST-02-ALIGNMENT.md
autonomous: true
requirements:
  - TRUST-02
user_setup: []
must_haves:
  truths:
    - EN, RU, and TH do not contradict on 1,000mg / product class for Ingot and Profound intensity note (same pass)
    - `.planning/phases/10-claims-counsel-alignment/10-TRUST-02-ALIGNMENT.md` exists and records review scope + honest counsel/alignment status
  artifacts:
    - 10-TRUST-02-ALIGNMENT.md
  key_links:
    - index Ingot copy ↔ i18n `intensity_profound_note` ↔ bespoke intensity UI
---

<objective>
Unify product-adjacent wording (1,000mg, cannabis/botanical framing) between collection Ingot, bespoke Profound label, and i18n dictionaries; add TRUST-02 alignment memo under `.planning/`.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
| T-10-01 | Inconsistent potency/product-class copy confuses users or suggests different SKUs | medium | One EN anchor; parallel RU/TH edits in same task; `rg` acceptance |
| T-10-02 | Memo implies counsel approval when not obtained | high | Template forces **Counsel status** field `Pending` until human updates to `Approved` with date |
| T-10-03 | Unintended new health/efficacy claims | medium | Edit-only existing claims; do not add superlatives or medical statements |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-claims-counsel-alignment/10-RESEARCH.md
@.planning/phases/10-claims-counsel-alignment/10-UI-SPEC.md
@.planning/phases/10-claims-counsel-alignment/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>TRUST-02 — align Ingot, Profound note, and i18n</name>
  <files>index.html, i18n.js</files>
  <read_first>index.html, i18n.js, bespoke.html, .planning/phases/10-claims-counsel-alignment/10-RESEARCH.md, .planning/REQUIREMENTS.md</read_first>
  <action>
**EN anchor (apply everywhere EN appears for these two surfaces):**
- In `index.html` Ingot `data-lang="en"` first paragraph: use exactly `1,000mg discreet cannabis-infused botanical extract.` (lowercase **c** in **cannabis**).
- In `i18n.js` under `en` set `intensity_profound_note` to exactly ` (1,000mg cannabis-infused botanical extract)` (leading space before parenthesis so it sits against “Profound” in UI; content mirrors Ingot product class without the word “discreet” in the parenthetical to keep the line short; **same** “cannabis-infused botanical extract” string as the Ingot line after the mg clause).

**RU:** Update Ingot `data-lang="ru"` and `i18n.js` `ru` `intensity_profound_note` so both describe the **same** product fact (1,000 мг, same role of cannabis/botanical) without one calling it only “инфузия” and the other a different chemical class. Use professional parallel to EN anchor; if RESEARCH found mismatch, **replace** RU to one coherent phrasing in both files.

**TH:** Update Ingot `data-lang="th"` and `i18n.js` `th` `intensity_profound_note` so both reflect the **same** product class (e.g. extract + cannabis) and **1,000 มก.** — do not leave one as “น้ำมันชง” and the other full extract if that implies a different form factor unless memo documents intentional difference (default: **no** — align).

**the-story.html:** Grep for `mg|กัญ|cannabis|каннаб`. If **no** matches, **do not** edit story; if matches appear, apply same glossary as above in one pass.
  </action>
  <verify>cd repo root; rg -n "intensity_profound_note|1,000mg|1000 мг|1,000 มก" index.html i18n.js bespoke.html</verify>
  <acceptance_criteria>
    - `index.html` contains substring `1,000mg discreet cannabis-infused botanical extract.`
    - `index.html` does not contain substring `Cannabis-infused` (capital C) in the Ingot EN line
    - `i18n.js` for key `intensity_profound_note` in `en` object contains substring `1,000mg cannabis-infused botanical extract`
    - `rg -n "intensity_profound_note" i18n.js` returns 3 language blocks (en, ru, th)
    - `rg "TODO" i18n.js` returns exit 1 (no new TODOs in this file) OR zero matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>TRUST-02 — alignment memo</name>
  <files>.planning/phases/10-claims-counsel-alignment/10-TRUST-02-ALIGNMENT.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/10-claims-counsel-alignment/10-RESEARCH.md, .planning/phases/03-trust-compliance/03-RESEARCH.md</read_first>
  <action>
Create **`.planning/phases/10-claims-counsel-alignment/10-TRUST-02-ALIGNMENT.md`** with markdown sections in this order:

1. **Jurisdiction** — e.g. Thailand as primary market (one sentence; adjust if product counsel specifies otherwise)
2. **Scope reviewed** — bullet list: `index.html` (Ingot), `bespoke.html` (Botanical Intensity), `i18n.js` (intensity + Ingot-related keys if touched), `the-story.html` (state “reviewed, no mg claims” or list edits)
3. **EN anchor** — quote the final EN strings for Ingot + `intensity_profound_note` (copy-paste from files after task 1)
4. **Method** — e.g. editorial alignment + TRUST-02 requirement; counsel review scheduled / completed (honest)
5. **Counsel status** — one of: `Pending` | `Approved YYYY-MM-DD` | `N/A (internal-only alignment)` **must** start as `Pending` unless you have real approval to write otherwise
6. **Outcome** — `Aligned copy` or `Pending review` (honest to actual state)
7. **Open items** — e.g. final counsel sign-off, TH nuance check

No fabricated legal sign-off.
  </action>
  <verify>test -f .planning/phases/10-claims-counsel-alignment/10-TRUST-02-ALIGNMENT.md</verify>
  <acceptance_criteria>
    - `test -f .planning/phases/10-claims-counsel-alignment/10-TRUST-02-ALIGNMENT.md` exits 0
    - `rg -n "^## " .planning/phases/10-claims-counsel-alignment/10-TRUST-02-ALIGNMENT.md` shows headers including `Scope reviewed` and `Counsel status`
    - `rg -n "Pending|Approved|N/A" .planning/phases/10-claims-counsel-alignment/10-TRUST-02-ALIGNMENT.md` returns ≥1 match in Counsel status context
  </acceptance_criteria>
</task>

</tasks>

## PLANNING COMPLETE

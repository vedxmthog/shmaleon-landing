---
phase: 01-site-shell-navigation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - index.html
  - bespoke.html
  - the-story.html
autonomous: true
requirements:
  - SOCIAL-01
  - FORM-01
user_setup:
  - Replace placeholder SOCIAL_* URLs with real Instagram/Telegram/WhatsApp destinations before production
must_haves:
  truths:
    - Footer exposes Instagram, Telegram, WhatsApp with configurable hrefs
    - Bespoke flow includes channel selector + validated contact field + non-server submit hand-off (mailto or wa.me)
  artifacts:
    - index.html
    - bespoke.html
    - the-story.html
  key_links:
    - Social hrefs centralized per file (constant block comment)
---

<objective>
Add **footer social links** (SOCIAL-01) and **bespoke contact capture** (FORM-01) with channel-specific validation and static submit hand-off (no backend).
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
| T-01 | XSS via engraving/contact in mailto body | medium | Build body with `encodeURIComponent` on sanitized strings; strip `&lt;` `>`; use template literals from validated fields only |
| T-02 | PII in URL length | low | Keep mailto body concise; warn if overlong |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md (FORM-01, SOCIAL-01, Out of Scope)
@.planning/phases/01-site-shell-navigation/01-UI-SPEC.md
@bespoke.html
@index.html
@the-story.html
</context>

<tasks>

<task type="auto">
  <name>SOCIAL_LINKS block + footer row</name>
  <files>index.html, bespoke.html, the-story.html</files>
  <read_first>index.html footer, bespoke.html footer, the-story.html footer</read_first>
  <action>Before `</body>` or in `<script>` first lines, define `const SOCIAL = { instagram: 'https://instagram.com/TODO', telegram: 'https://t.me/TODO', whatsapp: 'https://wa.me/TODO' };`. Footer: add three links opening in `rel="noopener noreferrer" target="_blank"` with visible labels EN (Phase 2 translates). Same structure all pages.</action>
  <verify>rg 'SOCIAL|instagram|t\\.me|wa\\.me' index.html</verify>
  <acceptance_criteria>
    - All three HTML files contain substring `instagram` in footer or script near footer
    - All three contain `t.me` or `telegram` literal in href or constant
    - All three contain `wa.me` or `whatsapp` literal in href or constant
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Bespoke contact channel UI + validation</name>
  <files>bespoke.html</files>
  <read_first>bespoke.html (sections 01–03 and script block)</read_first>
  <action>Add new section **04** (or integrate before submit): `<select id="contact-channel">` options `telegram|instagram|whatsapp|email`. Single `<input id="contact-value">` + `<label for="contact-value" id="contact-label">`. On channel change: update label text, `input.type` or `inputmode`, `placeholder`, and `pattern` attribute. On **INITIATE REQUEST** click: if invalid, set `aria-invalid=true` and show `#contact-error` span; else build `mailto:…` OR wa link: `if channel==whatsapp` open `https://wa.me/${digitsOnly}` with optional text; else `mailto:concierge@example.com?subject=Bespoke&body=...` (use placeholder email constant `BESPOKE_MAILTO` at top of script — replace later). Include form selection summary (form, intensity, inscription) in body.</action>
  <verify>Manual: invalid email shows error; valid toggles mail client or wa</verify>
  <acceptance_criteria>
    - `bespoke.html` contains `contact-channel` or `id="contact-channel"` OR name attribute `contact_channel`
    - `bespoke.html` script contains validation function or `checkValidity` call for submit button handler
    - `bespoke.html` contains placeholder string `concierge@` or `mailto:` in script
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Document hand-off in README or .planning</name>
  <files>README.md or .planning/deploy.md</files>
  <read_first>.planning/REQUIREMENTS.md Out of Scope</read_first>
  <action>Add 3–5 lines: bespoke submit uses client-only mailto/wa; replace `BESPOKE_MAILTO` and `SOCIAL` URLs before launch.</action>
  <verify>rg 'BESPOKE_MAILTO|mailto' README.md .planning/deploy.md 2>/dev/null || rg 'mailto' README.md</verify>
  <acceptance_criteria>
    - `README.md` contains word `mailto` or `wa.me` in context of bespoke / static hand-off
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Click each social link → resolves (even if TODO host)
- [ ] Submit bespoke with each channel type; body matches selection
</verification>

<success_criteria>
- SOCIAL-01 + FORM-01 covered
- No `fetch(` to unknown API introduced
</success_criteria>

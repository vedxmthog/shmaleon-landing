---
phase: 01-site-shell-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - bespoke.html
  - the-story.html
  - the-collection.html
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-03
  - SITE-04
user_setup: []
must_haves:
  truths:
    - Root URL serves collection markup via index.html
    - the-collection.html redirects to index.html
    - All pillar nav hrefs are real HTML files not hash-only
    - Join / priority list CTAs navigate to bespoke.html
  artifacts:
    - index.html
    - the-collection.html
  key_links:
    - index.html <-> bespoke.html <-> the-story.html
---

<objective>
Verify and complete Phase 1 **information architecture**: canonical home at `index.html`, legacy redirect stub, consistent nav targets and active-state classes per page, footer copyright consistency, priority CTAs → `bespoke.html`.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
| T-01 | Phishing via external nav URL | low | Pillar `href` only `index.html`, `bespoke.html`, `the-story.html` (relative) |
| T-02 | Stale content on wrong file | low | Grep each page for `href="#"` on pillar nav; zero occurrences |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@index.html
@bespoke.html
@the-story.html
@the-collection.html
</context>

<tasks>

<task type="auto">
  <name>Audit pillar hrefs and active nav states</name>
  <files>index.html, bespoke.html, the-story.html</files>
  <read_first>index.html, bespoke.html, the-story.html, .planning/REQUIREMENTS.md</read_first>
  <action>For each page: (1) Collection link `href="index.html"`. (2) Bespoke `href="bespoke.html"`. (3) Story `href="the-story.html"`. (4) Active page: that link uses `text-secondary font-medium border-b border-secondary/40 pb-1`; inactive uses `text-neutral-400` without bottom border. Fix any drift.</action>
  <verify>Visual: desktop nav three links go to real files</verify>
  <acceptance_criteria>
    - `rg 'href=\"index\\.html\"' index.html bespoke.html the-story.html` returns ≥3 matches total across files for collection link
    - `rg 'href=\"bespoke\\.html\"' index.html bespoke.html the-story.html` matches on all three files
    - `rg 'href=\"the-story\\.html\"' index.html bespoke.html the-story.html` matches on all three files
    - Desktop nav row: `rg 'hidden md:flex gap-8' -n index.html bespoke.html the-story.html` each file has matching block whose next 20 lines contain zero `href="#"` (executor verifies by read)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Verify redirect stub and Join / priority CTAs</name>
  <files>the-collection.html, index.html, the-story.html</files>
  <read_first>the-collection.html, index.html, the-story.html</read_first>
  <action>(1) `the-collection.html` must contain `location.replace("index.html")` or `url=index.html` refresh. (2) `index.html` scarcity CTA: outer element `href="bespoke.html"`. (3) `the-story.html` final CTA: single `a[href="bespoke.html"]` wrapping localized spans.</action>
  <verify>rg 'bespoke\\.html' index.html the-story.html</verify>
  <acceptance_criteria>
    - `the-collection.html` contains substring `index.html` in meta refresh or script replace
    - `index.html` contains `href="bespoke.html"` inside Join Priority section (rg -n 'Join Priority|bespoke' index.html)
    - `the-story.html` contains `href="bespoke.html"` adjacent to Priority List copy
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Footer SITE-03 alignment</name>
  <files>index.html, bespoke.html, the-story.html</files>
  <read_first>.planning/PROJECT.md (footer / year decision)</read_first>
  <action>Ensure © string consistent across three pages (same year and BY INVITATION ONLY pattern per locale blocks). If PROJECT says update year, apply same year everywhere.</action>
  <verify>diff copyright lines or rg '©' three files</verify>
  <acceptance_criteria>
    - `rg '©' index.html bespoke.html the-story.html` each file has exactly one footer copyright occurrence with matching brand string SHMALEON
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `npx --yes serve -l 3456` — open `/`, click each nav link, click Join/priority CTA → lands on bespoke
- [ ] Open `/the-collection.html` → lands on home content
</verification>

<success_criteria>
- All three plans’ SITE-* requirements satisfied by grep + smoke
- No regressions to Tailwind config blocks
</success_criteria>

---
phase: 01-site-shell-navigation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - index.html
  - bespoke.html
  - the-story.html
autonomous: true
requirements:
  - NAV-01
  - CHROME-01
  - I18N-03
user_setup: []
must_haves:
  truths:
    - Below md breakpoint burger opens/closes panel with same links as desktop
    - SHMALEON title does not overlap burger or locale controls at 375px width
    - Language choice uses one disclosure control (not three always-visible pills on mobile)
    - shopping_bag and person icons absent sitewide in chrome
  artifacts:
    - index.html
    - bespoke.html
    - the-story.html
  key_links:
    - Panel links match desktop href targets
---

<objective>
Implement **mobile-first chrome**: functional burger + accessible panel, collapsed locale picker (click/touch, not hover-only), responsive padding/title layout to fix overlap, remove any remaining commerce icons, verify CHROME-01 sitewide.
</objective>

<threat_model>
| ID | Threat | Severity | Mitigation |
| T-01 | Focus lost / keyboard trap | medium | On menu close, return focus to burger; visible focus ring |
| T-02 | aria spoofing | low | Static site; no user-generated chrome |
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-site-shell-navigation/01-UI-SPEC.md
@.planning/phases/01-site-shell-navigation/01-RESEARCH.md
@.planning/phases/01-site-shell-navigation/01-PATTERNS.md
@index.html
@bespoke.html
@the-story.html
</context>

<tasks>

<task type="auto">
  <name>Remove commerce icons sitewide (CHROME-01)</name>
  <files>index.html, bespoke.html, the-story.html</files>
  <read_first>index.html, bespoke.html, the-story.html</read_first>
  <action>Delete all `shopping_bag` and `person` Material Symbol spans and wrapping anchors from nav trailing cluster on each page if any remain after 01-01.</action>
  <verify>rg 'shopping_bag|>person<' index.html bespoke.html the-story.html || true</verify>
  <acceptance_criteria>
    - `rg shopping_bag index.html bespoke.html the-story.html` exits 1 (no matches)
    - `rg 'material-symbols-outlined.*person' index.html bespoke.html the-story.html` exits 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Burger + panel with pillar links (NAV-01)</name>
  <files>index.html, bespoke.html, the-story.html</files>
  <read_first>index.html (nav block), bespoke.html (nav block), the-story.html (nav block)</read_first>
  <action>On each page: wire existing `md:hidden` menu button to toggle a panel (`hidden` class or `aria-hidden`) containing three links with **same href + active styling** as desktop row. Add `aria-expanded` on button; `id` on panel; `aria-controls` linking button→panel. Optional: ESC closes. Adjust outer nav flex/grid so at `max-width: 375px` title and controls do not overlap (e.g. grid 3-col, smaller logo text, or logo only in panel on mobile per UI-SPEC).</action>
  <verify>Manual 375px + keyboard</verify>
  <acceptance_criteria>
    - Each of the three HTML files contains `aria-expanded` on burger button
    - Each contains `id=` on mobile nav panel container referenced by `aria-controls`
    - `rg 'md:hidden.*menu' index.html bespoke.html the-story.html` finds button on each file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Locale disclosure (I18N-03) — mobile safe</name>
  <files>index.html, bespoke.html, the-story.html</files>
  <read_first>bespoke.html (current group-hover dropdown), index.html (lang buttons), the-story.html (lang buttons)</read_first>
  <action>Replace always-visible triple EN|RU|TH strip on `<md` with one button showing current locale that toggles list/menu (click). Desktop may keep compact control; must not rely on `:hover` alone for opening. Reuse each page’s existing `setLanguage` / `switchLanguage` functions; only change DOM structure + listeners.</action>
  <verify>Tap test on iOS simulator or device</verify>
  <acceptance_criteria>
    - On `index.html`, mobile header does not contain three separate visible `.lang-btn` or `data-lang-btn` siblings without a wrapping disclosure — acceptable: one button + hidden menu
    - `bespoke.html` language UI uses `click` or `focusin` pattern: grep for `onclick` or `addEventListener` on locale control OR native select
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Responsive overlap audit (hero + sections)</name>
  <files>index.html, bespoke.html, the-story.html</files>
  <read_first>index.html (hero `min-h`, `pt-20`), the-story.html hero, bespoke.html hero</read_first>
  <action>At minimum: reduce nav `px-12 py-10` to `px-4 py-6 md:px-12 md:py-10` if overlap persists. Adjust `index.html` hero `min-h-[800px]` + `pt-20` so first headline clears fixed nav on iPhone SE. Document tweaks in SUMMARY.md.</action>
  <verify>Chrome device toolbar 375×667 screenshot sanity</verify>
  <acceptance_criteria>
    - `index.html` nav outer flex includes responsive padding classes `px-4` or `sm:px-6` before `md:px-12`
    - Hero section has `pt-` at least `24` (Tailwind `pt-24`) or equivalent after change for small screens
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] 375px: open/close menu; pick locale; no horizontal scroll
- [ ] Lighthouse a11y score not regressed vs baseline (manual note)
</verification>

<success_criteria>
- NAV-01, CHROME-01, I18N-03 acceptance met
- No console errors on toggle
</success_criteria>
